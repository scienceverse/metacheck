.Rproj.user
inst/doc
.Rhistory
.Rdata
.httr-oauth
.DS_Store
.quarto

inst/python/constants.py
inst/modules/sample-size*
inst/templates/_report.html
/doc/
/Meta/

# pkgdown website: built and deployed to the gh-pages branch by CI
# (.github/workflows/pkgdown.yaml), not committed to source branches.
/docs/

# Generated by data-raw/report-example.R; re-rendered by the pkgdown build
# rather than committed, so re-running the script doesn't add a new full-size
# blob to history every time.
/pkgdown/assets/report-example.html
/pkgdown/assets/report-example.qmd

/data-raw/cache/
_stuff/modules-old/sample-size*
_stuff/check_correct_citations/pdfs_lakens
_stuff/check_correct_citations/xmls_lakens
_stuff/report-demo/

.claude/
CLAUDE.md


.positai

# metacheck's on-disk caches (downloaded repository files and LLM responses).
# Created in the working directory by default, so they appear wherever code is
# run from; see metacheck_cache_info() to locate them or the metacheck.cache.dir
# option to move them elsewhere.
#
# Only ignored at the repository root. NOT a blanket `.metacheck_*`: the folder
# tests/testthat/.metacheck_repo_cache holds COMMITTED ResearchBox fixtures that
# a blanket pattern would hide, so a future deletion of them would go unnoticed.
/.metacheck_repo_cache/
/.metacheck_llm_cache/

.comments/
