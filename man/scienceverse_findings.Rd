% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scienceverse-TUE051134.R, R/scienceverse.R
\name{scienceverse_findings}
\alias{scienceverse_findings}
\title{Search findings in a scienceverse archive}
\usage{
scienceverse_findings(
  con,
  table = "stat_findings",
  text = "",
  ranges = list(),
  doi = NULL,
  limit = 1000
)

scienceverse_findings(
  con,
  table = "stat_findings",
  text = "",
  ranges = list(),
  doi = NULL,
  limit = 1000
)
}
\arguments{
\item{con}{a scienceverse connection.}

\item{table}{one of \code{stat_findings}, \code{code_findings}, \code{data_findings},
\code{excel_findings}, \code{other_findings}.}

\item{text}{free-text matched against the finding \code{text} (per-term, AND).}

\item{ranges}{a named list of \code{c(min, max)} (either may be \code{NA}) for numeric
columns of \code{table}; unknown columns are ignored.}

\item{doi}{optional DOI to restrict to one paper.}

\item{limit}{maximum rows to return.}
}
\value{
a data frame of matching findings.

a data frame of matching findings.
}
\description{
Query one findings table, optionally with a free-text match on the finding
text and numeric range filters on that table's numeric columns. This is the
engine behind the app's Findings tab (e.g. stat findings with \code{f_reported}
greater than 5).

Query one findings table, optionally with a free-text match on the finding
text and numeric range filters on that table's numeric columns. This is the
engine behind the app's Findings tab (e.g. stat findings with \code{f_reported}
greater than 5).
}
