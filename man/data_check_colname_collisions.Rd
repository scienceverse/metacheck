% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_check_helpers.R
\name{data_check_colname_collisions}
\alias{data_check_colname_collisions}
\title{Flag column names that collide after sanitization}
\usage{
data_check_colname_collisions(col_names)
}
\arguments{
\item{col_names}{character vector of a file's column names}
}
\value{
a named list mapping each colliding column name to a message
(empty list when all names stay distinct)
}
\description{
Many tools replace the special characters in a variable name with \verb{_} or
drop them: R's \code{make.names()}, SPSS/SAS/Stata on import, and generated
codebooks (section ids, figure file names). Two columns whose names differ
\emph{only} in special characters -- e.g. the phoneme symbols \verb{t'} and a
t-with-diacritic, which both sanitize to \code{t_} -- therefore become
indistinguishable the moment the data leave the original file, and links or
merged results silently point at the wrong variable. Identical duplicate
names collide trivially and are flagged too.
}
\details{
The sanitization mirrored here (every character that is not a Unicode
letter or digit becomes \verb{_}) is the one the codebook package uses for its
section ids, where collisions surface as pandoc "Duplicate identifier"
warnings.
}
\keyword{internal}
