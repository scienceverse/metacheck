% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stata.R
\name{import_stata_smcl}
\alias{import_stata_smcl}
\title{Read a Stata Markup and Control Language (.smcl) output log}
\usage{
import_stata_smcl(path)
}
\arguments{
\item{path}{path to a \code{.smcl} file}
}
\value{
a list of result tables, each \code{list(analysis, title, data, syntax, table_index)} -- the same shape \code{\link[=read_stat_tables]{read_stat_tables()}} returns
for \code{.jasp}/\code{.omv}/\code{.spv}, so all four formats can be processed
identically downstream. \code{analysis} is the Stata command that produced
the table (e.g. \code{"summarize communication_index barrier_index"});
\code{syntax} duplicates \code{analysis} (Stata's own echoed command IS its
syntax, unlike \code{.spv} where syntax is recovered separately from a
different structure). Empty list if the file has no recoverable
command output.
}
\description{
Renders the file's markup to plain text (see \code{\link[=.smcl_render]{.smcl_render()}}), splits it
into one chunk per Stata command (the \verb{\{com\}. <command>} echo, mirroring
\code{\link[=read_r_output]{read_r_output()}}'s R-console-echo splitting), and extracts each chunk's
result tables and one-line statistics. \verb{graph export "....png"} commands
are recovered as ordinary command text; the PNG itself is never embedded
in a \code{.smcl} file (Stata writes it to disk separately), so no chart data
exists in this format to extract (see the file header).
}
