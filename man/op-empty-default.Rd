% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/svutils-utils.R
\name{op-empty-default}
\alias{op-empty-default}
\alias{\%empty_or\%}
\title{Default value for \code{NULL} or length-zero values}
\usage{
x \%empty_or\% y
}
\arguments{
\item{x, y}{If \code{x} is \code{NULL} or has length 0, will return \code{y}; otherwise returns \code{x}.}
}
\description{
Like \code{\link{\%||\%}}, but also substitutes \code{y} when \code{x} has length zero (for
example an empty array in a parsed JSON API response), not only when \code{x}
is \code{NULL}. This matters when \code{x} feeds a single \code{data.frame()} column
alongside scalar values: a length-zero \code{x} (as opposed to \code{NULL}) passes
\verb{\%||\%} unchanged and then throws "arguments imply differing number of
rows" (in \code{data.frame()}) or "replacement has 0 rows" (in \verb{$<-}), which
typically aborts and silently drops the whole record it belongs to.
}
\examples{
1 \%empty_or\% 2
NULL \%empty_or\% 2
character(0) \%empty_or\% 2
}
\keyword{internal}
