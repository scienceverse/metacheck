% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract-tests.R
\name{extract_tests}
\alias{extract_tests}
\title{Extract the statistical tests a paper reports}
\usage{
extract_tests(paper)
}
\arguments{
\item{paper}{a paper object (uses its \verb{$eq} and \verb{$text})}
}
\value{
a data.frame, one row per reported test, with \code{paper_id},
\code{test_no} (sequential within the paper), \code{text_id}, \code{paragraph_id},
\code{section_id}, \code{sentence} (the reporting sentence), \code{anchor} (the test
statistic the report is built around, e.g. \code{"t"}), \code{n_components},
\code{components} (a list-column of \code{name}/\code{comp}/\code{value}/\code{df}), and \code{reported}
(the test rendered back as text, e.g. \code{"t(23) = 3.77, p = .001, d = 0.77"}).
}
\description{
Builds the paper-side counterpart to the analysis output that
\code{reproducibility_check} extracts: one row per reported TEST, with its
components kept together and the sentence it was reported in retained, so a
matched result can be traced back to the claim it supports.
}
\details{
This is a purpose-built alternative to \code{\link[=extract_eq]{extract_eq()}} for matching. It reuses
\code{extract_eq()}'s parsing of \verb{name <op> value} fragments, then does three
things that parsing does not:
\itemize{
\item \strong{isolates separate tests} within one sentence. \code{extract_eq()}'s \code{grp_id}
is the sentence number, so a sentence reporting two t-tests yields a single
ten-component "test" that no analysis can produce; here a repeated anchor
(a second \code{t}, \code{F}, \code{r}, ...) starts a new test;
\item \strong{keeps the sentence}, its \code{text_id}, \code{paragraph_id} and \code{section_id}, so a
match links a \code{result_id} to the sentence making the claim;
\item \strong{filters non-statistics}, so a figure's \code{height = 4} or a sample's
\code{range = 18-29} is not offered as evidence. A run of components with no
recognised test statistic in it is dropped entirely.
}
}
