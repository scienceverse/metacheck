% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_check_helpers.R
\name{match_column_labels}
\alias{match_column_labels}
\title{Match data columns against codebook variable definitions (rules only)}
\usage{
match_column_labels(columns_df, codebook_vars_df)
}
\arguments{
\item{columns_df}{data columns to label (needs \code{paper_id}, \code{source_file},
\code{column_name}; optional \code{group}/\code{experiment_group})}

\item{codebook_vars_df}{parsed codebook variables (from \code{\link[=parse_codebook]{parse_codebook()}})}
}
\value{
a data.frame with one row per input column: \code{paper_id},
\code{source_file}, \code{column_name}, \code{group}, \code{label}, \code{codebook_variable},
\code{label_source}, \code{label_status}, \code{label_method}.
}
\description{
For each column in \code{columns_df}, find codebook variables whose normalised
name matches, respecting experiment-group scoping. Resolves multiple
definitions by haven priority, then rule-based label-equivalence
(\code{normalize_label}); genuinely differing labels are flagged
\code{conflicting_definition}. The LLM tiers (fuzzy matching, semantic merge) are
applied separately by codebook_check when \code{llm_use(TRUE)}.
}
\keyword{internal}
