% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/archive-4tu.R
\name{researchdata4tu_pat}
\alias{researchdata4tu_pat}
\title{Set or get a 4TU.ResearchData API token}
\usage{
researchdata4tu_pat(pat = NULL)
}
\arguments{
\item{pat}{the token to set, or NULL to get the current token}
}
\value{
the current token (character; \code{""} when none is set)
}
\description{
4TU.ResearchData issues its own personal access tokens (from account
settings on data.4tu.nl) -- a Figshare.com token is meaningless here, so
this is stored separately from \code{\link[=figshare_pat]{figshare_pat()}}. A token is optional for
reading public articles (used here only to raise rate limits or read
private ones).
}
\details{
Store it as the \code{RESEARCHDATA4TU_PAT} environment variable so it is read
every time R starts.
}
\examples{
researchdata4tu_pat() # returns "" unless a token is set
}
