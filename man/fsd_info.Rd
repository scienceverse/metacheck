% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/archive-fsd.R
\name{fsd_info}
\alias{fsd_info}
\title{Retrieve info from FSD by URL}
\usage{
fsd_info(fsd_url, id_col = 1, pb = NULL, cache = FALSE)
}
\arguments{
\item{fsd_url}{an FSD URL (catalogue page, DOI, or URN), or a table
containing them (e.g., as created by \code{fsd_links()})}

\item{id_col}{the index or name of the column that contains FSD URLs, if
\code{fsd_url} is a table}

\item{pb}{a progress bar passed from another function}

\item{cache}{if \code{TRUE}, reuse a cached listing from a prior call for the
same study instead of re-querying FSD (see \code{\link[=repo_info_cache]{repo_info_cache()}});
default \code{FALSE}}
}
\value{
a data frame of information
}
\description{
Lists a Finnish Social Science Data Archive study's title, DOI, access
restriction, and data-file inventory (name, format, case/variable counts)
via its open DDI-C 2.5 XML metadata record. There is no file-level
download here -- the DDI record carries no download URI, and actual data
access requires Haka/Aila login or a per-dataset access application (see
the file header comment) -- so \code{files} is for REPORTING what a study
contains, not for \code{download_repo_files()} to fetch.
}
\examples{
\dontrun{
fsd_info("https://services.fsd.tuni.fi/catalogue/FSD2653")
}
}
