% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/archive-report.R
\name{collect_check_results}
\alias{collect_check_results}
\title{Collect a run's check results into corpus-wide CSVs}
\usage{
collect_check_results(results_dir, out_dir = NULL)
}
\arguments{
\item{results_dir}{directory holding the per-paper \verb{*.checks.json} files.}

\item{out_dir}{directory to write the two CSVs into. \code{NULL} (default) uses
\code{results_dir}.}
}
\value{
invisibly, a list with \code{checks} and \code{findings} data frames.
}
\description{
Reads every \verb{<paper_id>.checks.json} in \code{results_dir} (written by
\code{\link[=capture_check_results]{capture_check_results()}}) and produces two flat tables:
}
\details{
\itemize{
\item \verb{_all_checks.csv} — one row per paper per module: the module's
\code{traffic_light}, its \code{summary_text}, and its per-paper count columns
(unpacked from the module's \code{summary_table}, so e.g. \code{code_check}'s
\code{code_n} or \code{data_validate}'s \code{flagged_n} become their own columns).
\item \verb{_all_findings.csv} — one row per individual flagged item across all papers
and modules (the row-level \code{table} each module returns), tagged with
\code{paper_id} and \code{module}.
}

Like \code{\link[=write_file_manifest]{write_file_manifest()}}, it reads only on-disk artifacts, so it can be
run at any time after (or during) a batch.
}
\examples{
\dontrun{
collect_check_results("output/openmind/_checks")
}
}
\seealso{
\code{\link[=capture_check_results]{capture_check_results()}}, \code{\link[=write_file_manifest]{write_file_manifest()}}
}
