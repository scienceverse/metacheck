% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_check_helpers.R
\name{text_peek}
\alias{text_peek}
\title{Peek at the first lines of a text file}
\usage{
text_peek(path, n = 20L)
}
\arguments{
\item{path}{path to a file.}

\item{n}{maximum number of lines to read.}
}
\value{
a character vector of at most \code{n} lines (UTF-8), or \code{character(0)}.
}
\description{
Reads the first \code{n} lines of a file as text, tolerating the encodings research
data actually ships in: a UTF-8/UTF-16 BOM, UTF-16 (E-Prime exports), and
Latin-1 bytes that would otherwise make \code{readLines()} output error in later
string operations. Intended for cheap format sniffing — deciding \emph{what} a file
is before committing to a reader — not for reading data.
}
\details{
Returns \code{character(0)} for a missing, empty or unreadable file rather than
erroring, so a caller can treat "cannot peek" as "not my format".
}
\examples{
f <- tempfile(fileext = ".txt")
writeLines(c("*** Header Start ***", "Experiment: naming"), f)
text_peek(f, n = 2)
}
\keyword{internal}
