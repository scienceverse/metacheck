% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spv.R
\name{.spv_export_syntax}
\alias{.spv_export_syntax}
\title{Recover an .spv file's SPSS syntax as a sibling .sps file}
\usage{
.spv_export_syntax(spv_path, code_dir_name = "code")
}
\arguments{
\item{spv_path}{path to the \code{.spv} file.}

\item{code_dir_name}{name of the sibling code subdirectory to write into,
relative to \code{spv_path}'s own directory. Default \code{"code"}.}
}
\value{
the path to the written \code{.sps} file, or \code{NA_character_} if the
archive has no recoverable syntax (e.g. every command was skipped by
this port's "core tables only" scope, or the file has no structure XML
at all).
}
\description{
A \code{.spv} file is SPSS's rendered OUTPUT, but its structure XML embeds the
exact syntax that produced each result (see \code{\link[=.spv_read_structure]{.spv_read_structure()}},
\code{\link[=.spv_read]{.spv_read()}}'s \code{syntax} field) — the same commands a researcher would
normally save separately as a \code{.sps} syntax file, but here recovered from
the output alone. Since \code{.spv} is classed \code{data_type = "output"} (see
\code{.data_check_types()} / \code{.data_check_type()} in R/data_check_helpers.R),
this materialises that recovered syntax as real CODE, in a \code{code}
subdirectory alongside the original \code{.spv} file, so it is discoverable
the same way an author's own saved \code{.sps} file would be — and, crucially,
so it then flows through \code{code_check()}'s ordinary SPSS-language analysis
(comments, absolute paths, library lines, ...) unmodified, since
\code{code_lang()} already recognises \code{.sps} (see \code{.code_expand_spv()} in
R/code_check.R, this function's only caller).
}
\keyword{internal}
