% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/archive-psycharchives.R
\name{psycharchives_file_download}
\alias{psycharchives_file_download}
\title{Retrieve public file list from PsychArchives by URL}
\usage{
psycharchives_file_download(pa_url, pb = NULL, cache = FALSE)
}
\arguments{
\item{pa_url}{a vector of PsychArchives URLs}

\item{pb}{a progress bar passed from another function}

\item{cache}{if \code{TRUE}, reuse a cached listing from a prior call for the
same item instead of re-querying PsychArchives (see
\code{\link[=repo_info_cache]{repo_info_cache()}}); default \code{FALSE}}
}
\value{
a data frame of file information (one row per public bitstream)
}
\description{
Lists the publicly retrievable bitstreams of one or more PsychArchives items
via the DSpace REST API, without downloading them. Each row carries an
absolute \code{file_url} (the bitstream retrieve endpoint) so the actual bytes are
fetched later by \code{\link[=download_repo_files]{download_repo_files()}}, the same deferred path used for
Zenodo and OSF. Restricted files are omitted by the API and never appear here.
}
