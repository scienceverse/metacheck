% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/archive-zenodo-upload.R
\name{zenodo_upload}
\alias{zenodo_upload}
\title{Upload Folders to Zenodo}
\usage{
zenodo_upload(
  folders,
  sandbox = TRUE,
  zenodo_pat = NULL,
  publish = FALSE,
  license = "cc-by-4.0",
  upload_type = "dataset",
  metadata = NULL,
  max_file_size = NULL,
  ask = TRUE,
  pb = NULL
)
}
\arguments{
\item{folders}{a data frame returned by \code{\link[=osf_file_download]{osf_file_download()}}, or a character
vector of paths to folders to upload (one deposition per folder)}

\item{sandbox}{whether to upload to the Zenodo sandbox (the default) rather
than the real zenodo.org}

\item{zenodo_pat}{a Zenodo personal access token. Defaults to whatever
\code{\link[=zenodo_pat]{zenodo_pat()}} returns for this server. See \code{\link[=zenodo_pat]{zenodo_pat()}} for how to
create one.}

\item{publish}{whether to publish each deposition. \code{FALSE} (the default)
leaves it as a draft for you to check and publish by hand.}

\item{license}{the Zenodo license identifier to use when the OSF project has
no license Zenodo recognises}

\item{upload_type}{the Zenodo resource type (e.g. \code{"dataset"},
\code{"publication"}, \code{"software"})}

\item{metadata}{a list of extra metadata fields to add to every deposition,
overriding what was taken from the OSF}

\item{max_file_size}{largest file to upload, in MB; larger files are skipped
and reported. Set to NULL for no limit.}

\item{ask}{whether to show a summary and ask for confirmation before
uploading anything. Ignored in a non-interactive session, where the upload
proceeds without asking.}

\item{pb}{a progress bar passed from another function}
}
\value{
a data frame with one row per folder: the folder path, the Zenodo
deposition id, its DOI, the URL to review it, how many files were uploaded
and skipped, and whether it was published
}
\description{
Creates a Zenodo deposition for each folder, uploads every file in it, and
attaches metadata. Designed to take the output of \code{\link[=osf_file_download]{osf_file_download()}}
directly, so a whole OSF account can be archived on Zenodo in two steps:
}
\details{
\if{html}{\out{<div class="sourceCode">}}\preformatted{osf_file_download("4i578", download_to = "my_osf") |>
  zenodo_upload()
}\if{html}{\out{</div>}}
}
\section{Safety}{
Uploads go to the Zenodo \strong{sandbox} by default. The sandbox is a complete
separate copy of Zenodo, run for testing at \url{https://sandbox.zenodo.org}. It
has its own accounts and its own tokens, and the DOIs it mints use a test
prefix that never resolves publicly, so nothing you do there is permanent.
Set \code{sandbox = FALSE} only once a sandbox run looks right.

Depositions are left as unpublished \strong{drafts} (\code{publish = FALSE}). A draft
can be edited or deleted freely. Review each draft in your Zenodo account,
then press Publish there when you are satisfied. Publishing on the real
zenodo.org is irreversible: the record and its DOI are permanent and cannot
be deleted.
}

\examples{
\dontrun{
# download one OSF project and put it on the sandbox as a draft
osf_file_download("6nt4v", download_to = "archive") |>
  zenodo_upload()

# upload a folder you already have
zenodo_upload("archive/6nt4v")

# once the sandbox run looks right, upload for real (still a draft)
zenodo_upload("archive/6nt4v", sandbox = FALSE)
}
}
