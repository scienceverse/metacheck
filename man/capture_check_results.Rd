% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/archive-report.R
\name{capture_check_results}
\alias{capture_check_results}
\title{Save one paper's check results to disk}
\usage{
capture_check_results(chain, results_dir, paper_id = NULL)
}
\arguments{
\item{chain}{a module-output chain from \code{\link[=report_module_run]{report_module_run()}}.}

\item{results_dir}{directory to write the per-paper \verb{*.checks.json} into
(created if needed).}

\item{paper_id}{optional paper id, used only if it cannot be recovered from
the chain's summary tables.}
}
\value{
the written path, invisibly.
}
\description{
Flattens a module-output chain (the result of \code{\link[=report_module_run]{report_module_run()}} or
\code{\link[=report]{report()}}) into per-paper check summaries and findings, and writes them as
\verb{<paper_id>.checks.json} in \code{results_dir}. Call it inside a batch loop right
after running the checks for a paper, so the check outcomes survive the run
(the in-memory chain is otherwise discarded once the archive is built) and an
interrupted run keeps everything processed so far.
}
\seealso{
\code{\link[=collect_check_results]{collect_check_results()}}
}
