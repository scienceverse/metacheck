% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/code_check.R
\name{code_setwd}
\alias{code_setwd}
\title{Find setwd() calls in code}
\usage{
code_setwd(code_text)
}
\arguments{
\item{code_text}{the code text for a single file (character vector), ideally
comment-free (as produced by \code{\link[=code_remove_comments]{code_remove_comments()}})}
}
\value{
a data frame with columns \code{setwd_call} (the \code{setwd(...)} text as
written) and \code{line} (its line number). Empty frame when none are found.
}
\description{
A \code{setwd()} call in analysis code is a portability problem: it hardcodes an
assumption about the working directory the code runs in (frequently an
absolute path on the author's own machine), mutates global state, and makes a
script depend on being run from a particular place. Best practice is to keep
the working directory as the caller sets it and use relative paths. This scans
the (comment-free) code for \code{setwd(...)} calls and returns one row per call,
with the argument as written. R-only (the construct is R's; other languages
have their own, e.g. Stata \code{cd}, which this does not scan).
}
\examples{
code_text <- c(
  "setwd('D:/Dropbox/project')",
  "x <- read.csv('data.csv')"
)
code_setwd(code_text)
}
