% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trove-index.R
\name{trove_parse_query}
\alias{trove_parse_query}
\title{Parse a faceted search query}
\usage{
trove_parse_query(query, fields = character())
}
\arguments{
\item{query}{the raw query string}

\item{fields}{character vector of valid field names. A \verb{field:} token whose
field is not in this list is treated as a free term (so a stray colon does
not silently drop the term).}
}
\value{
a list with \code{field_terms} (named list: field -> character vector of
terms) and \code{free_terms} (character vector)
}
\description{
Split a search string into field-scoped terms and free terms. A token of the
form \code{field:term} (e.g. \code{scale:panas}, \code{level:ordinal}) restricts that term
to the named field; a bare token (e.g. \code{trust}) is a free term matched across
all searchable fields. Quote a value to include spaces: \code{scale:"just world"}.
All terms are AND-combined.
}
\keyword{internal}
