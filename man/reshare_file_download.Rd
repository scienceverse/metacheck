% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/archive-reshare.R
\name{reshare_file_download}
\alias{reshare_file_download}
\title{Download all files from a ReShare deposit}
\usage{
reshare_file_download(
  reshare_id,
  download_to = ".",
  max_file_size = 10,
  max_download_size = 100,
  unzip_types = NULL,
  pb = NULL
)
}
\arguments{
\item{reshare_id}{a ReShare eprint ID, URL, or DOI}

\item{download_to}{path to download to}

\item{max_file_size}{maximum file size to download (in MB) - set to NULL or
Inf for no restrictions}

\item{max_download_size}{maximum total size to download - set to NULL or
Inf for no restrictions}

\item{unzip_types}{file categories to extract from \code{.zip} files in the
deposit rather than downloading the zip whole, named as
\code{\link[=data_classify_files]{data_classify_files()}} names them: \code{"data"}, \code{"code"}, \code{"materials"},
\code{"documentation"}, \code{"output"}, \code{"unknown"}. More than one may be given.
\code{NULL} (the default) downloads zips whole and leaves them zipped, as
before. \code{max_file_size} applies to each extracted member rather than to
the archive.}

\item{pb}{a progress bar passed from another function}
}
\value{
data frame of file info. When members are extracted from a zip, its
row reports the zip with \code{extracted} giving the number of members written;
\code{size_on_disk} and \code{checksum_ok} are then \code{NA}, because what is on disk is
the members rather than the archive ReShare published a size and MD5 for.
}
\description{
Creates a directory for the deposit and downloads all of its files.
Returns (invisibly) a data frame with file info.
}
\details{
Like Figshare, ReShare's EPrints REST API documents no whole-deposit
bulk-download endpoint -- each file is served from its own
\verb{.../id/file/<fileid>} URL, so this always downloads file by file. A
\code{.zip} already present in the deposit (ReShare's own documents are
frequently zips of related files, e.g. all interviews for one country) is
still handled the same way \code{\link[=dataverse_file_download]{dataverse_file_download()}} handles one:
fetched whole and left as a zip unless \code{unzip_types} asks for only
specific members out of it.

You can limit downloads to only files under a specific size (defaults to
10MB) and only a maximum download size (largest files will be omitted until
total size is under the limit). Omitted files will be listed as messages in
verbose mode, and included in the returned data frame with the \code{downloaded}
column value set to FALSE.
}
\examples{
\dontrun{
  reshare_file_download("854001")

  # take only the data files out of any zip in the deposit
  reshare_file_download("854001", unzip_types = "data")
}
}
