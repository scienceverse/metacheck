% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reproducibility_check.R
\name{repro_write_scripts}
\alias{repro_write_scripts}
\title{Write path-rewritten scripts into the materialised layout}
\usage{
repro_write_scripts(code_text_list, rewrite_list, plan, root)
}
\arguments{
\item{code_text_list}{named list of per-file code text (names are file_names)}

\item{rewrite_list}{named list of \code{repro_rewrite_paths()} results, same order}

\item{plan}{the \code{psychds_check} table (to find each script's own target_path)}

\item{root}{the materialised layout root}
}
\value{
a data frame with \code{file_name}, \code{script_path} (absolute path written),
\code{run_dir} (the working directory a run should use — always \code{root}),
\code{setwd_removed} (count of \code{setwd()} lines commented out),
\code{setwd_paths} (comma-joined paths those calls named, for the warning), and
\code{family_replaced} (count of \verb{family =}/\verb{base_family =} named-font
arguments replaced with \code{"sans"}, so a plot call does not fail with
"invalid font type" for a font not registered on the machine running the
sandbox).
}
\description{
Each script's referenced data paths are rewritten to their Psych-DS target
(via the per-file \code{repro_rewrite_paths()} result) so that, run from \code{root},
the reads/writes resolve against the materialised tree. The rewritten script
text is written to \verb{root/<script target_path>} (or, when the plan has no
target for the script itself, to \verb{root/<basename>} at the top level so it is
still runnable). A reference left ambiguous or unmatched is written unchanged
— the static report already flagged it.
}
\details{
A \code{setwd()} call in analysis code is bad practice: it hardcodes an assumption
about where the code runs (often an absolute path on the author's own machine,
e.g. \code{setwd("D:/Dropbox/...")}), and it overrides the working directory the
runner sets to the materialised layout — so the script's relative reads then
resolve against a directory that does not exist here, and the reproduction
fails for a reason that has nothing to do with the analysis. To run the code
we therefore \strong{comment out every \code{setwd()} call} (recorded per file in
\code{setwd_removed} and flagged in the report). The transform is in the same
spirit as the path rewriting: it makes the author's script runnable in its new
location without changing the analysis. A script that only reproduces after a
\code{setwd()} is removed is reported as "reproducible after ignoring a \code{setwd()}
that should not have been in the code and needs to be fixed".
}
