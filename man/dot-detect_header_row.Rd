% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_check_helpers.R
\name{.detect_header_row}
\alias{.detect_header_row}
\title{Locate the true header row of a table read WITHOUT a header}
\usage{
.detect_header_row(rows, max_scan = 4L)
}
\arguments{
\item{rows}{a list of character vectors (one per physical row), all the same
length; typically the first \code{max_scan + 1} rows of a headerless read.}

\item{max_scan}{how many leading rows to consider stripping as metadata.}
}
\value{
a list: \code{header_row} (1-based index of the true header, 1 = already
correct / no offset), \code{stripped} (character vectors of the metadata rows
above it), and \code{improved} (body type-consistency gained by the promotion).
}
\description{
Given the first several physical rows of a file (each a character vector, the
file read so that NO row was consumed as a header), find which row is the real
column header. Combines two signals, because no single one covers every file:
}
\details{
\enumerate{
\item \strong{Type-consistency of the body below.} A text/banner/units row on top of
numeric columns makes them look mixed; dropping it makes them cleanly
numeric (see \code{.numeric_col_fraction()}). Strong when the data are numeric
(the CDA/behavior sheets), weak when the body is mostly text.
\item \strong{Junk rows above.} A banner (one label repeated across columns) or a
near-empty spacer row is not a header (see \code{.is_junk_above_header()}). This
carries the cases where the numeric jump is weak -- e.g. the CDA banner row
of \code{CDA} x 110 sitting above a header that only relabels 3 of 113 columns.
}

The header is the SMALLEST \code{h} such that every row above \code{h} is junk, the body
below \code{h} is type-consistent, and promoting \code{h} does not make the typing worse.
When no leading row is junk, the header is row 1 (already correct).
}
\keyword{internal}
