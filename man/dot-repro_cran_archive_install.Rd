% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reproducibility_check.R
\name{.repro_cran_archive_install}
\alias{.repro_cran_archive_install}
\title{Install a package's most recent CRAN Archive version}
\usage{
.repro_cran_archive_install(pkg, install_lib, lib_dir)
}
\arguments{
\item{pkg}{the package name}

\item{install_lib}{the library to install into (the real library for
\code{cran_to_main_lib}, else the throwaway \code{lib_dir})}

\item{lib_dir}{the throwaway library (checked for loadability, same as the
caller's main install path)}
}
\value{
a list \code{ok} (logical), \code{msg} (character, error text on failure),
\code{version} (the Archive version installed, or NA)
}
\description{
Every package ever published to CRAN keeps its full version history under
the CRAN Archive (\verb{https://cran.r-project.org/src/contrib/Archive/<pkg>/}),
even after the live repository stops serving it. This lists that directory,
takes the most recent version by file modification time (the directory
listing carries no other reliable ordering), and installs the source
tarball directly — the one fallback worth trying automatically when a
CRAN-source dependency is not on live CRAN, since it needs no extra
service or paper-specific date, just the package name.
}
\keyword{internal}
