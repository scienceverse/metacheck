% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mplus.R
\name{.mplus_is_genuine_output}
\alias{.mplus_is_genuine_output}
\title{Check whether a .out file is genuinely Mplus output}
\usage{
.mplus_is_genuine_output(path)
}
\arguments{
\item{path}{path to a local file}
}
\value{
\code{TRUE} if the file's content is genuine Mplus output, \code{FALSE}
otherwise (including if it cannot be read at all)
}
\description{
\code{.out} is a very generic extension (also used for compiled Unix binaries,
generic tool logs, etc.), unlike \code{.spv}/\code{.smcl} which are unambiguously
SPSS/Stata-specific -- so, unlike those formats, a \code{.out} found in a
repository is not safely assumed to be Mplus by extension alone.
Classification (\code{data_classify_files()}/\code{.fixed_ext_type}) still keys on
the extension so a \code{.out} is downloaded in the first place (content can't
be checked before it's fetched), but this content check runs AFTER
download to catch and reclassify a false positive -- Mplus always prints
its own version banner as the file's first real line, so this is a cheap,
reliable real/not-real test.
}
\keyword{internal}
