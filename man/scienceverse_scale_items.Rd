% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scienceverse-TUE051134.R, R/scienceverse.R
\name{scienceverse_scale_items}
\alias{scienceverse_scale_items}
\title{Get the items of one scale}
\usage{
scienceverse_scale_items(con, code, doi = NULL)

scienceverse_scale_items(con, code, doi = NULL)
}
\arguments{
\item{con}{a scienceverse connection.}

\item{code}{the scale \code{code} (as shown in \code{\link[=scienceverse_scales]{scienceverse_scales()}}).}

\item{doi}{optional DOI to restrict to one paper's copy of the scale.}
}
\value{
a data frame with \code{item_id}, \code{item_type}, and \code{text} (the question),
ordered by item position. De-duplicated across papers by item id + text.

a data frame with \code{item_id}, \code{item_type}, and \code{text} (the question),
ordered by item position. De-duplicated across papers by item id + text.
}
\description{
Return the individual items (questions) of a scale, with their wording where
the OSD carried a translation. Backs the app's "items in this scale" table.

Return the individual items (questions) of a scale, with their wording where
the OSD carried a translation. Backs the app's "items in this scale" table.
}
