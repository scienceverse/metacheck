% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reproducibility_check.R
\name{repro_dependencies}
\alias{repro_dependencies}
\title{Collect the package dependencies a set of code files declare}
\usage{
repro_dependencies(code_text, lang = "R")
}
\arguments{
\item{code_text}{the code text for a single file (character vector), OR a list
of such vectors (one per file) to pool across files}

\item{lang}{the language (only R declares installable packages here)}
}
\value{
a data frame with columns \code{package}, \code{source} (\code{cran}, \code{github},
\code{url}, or \code{base}), \code{ref} (the source path/ref for github/url, else NA), and
\code{base} (logical, TRUE for a base/recommended package). One row per distinct
package. Empty frame (same columns) when none are found.
}
\description{
Walks each code file with \code{\link[=code_library_names]{code_library_names()}} and returns the distinct
packages the paper's code loads, tagged with how each was referenced and
whether it is installable from CRAN, from a named GitHub/URL source, or is a
base/recommended package already shipped with R (so a run need not install
it). This is the input list a run would install; it is deliberately
name-only, because static analysis cannot know which \emph{version} was used —
running against the latest packages and observing a break is itself a
finding (it argues the authors should have pinned versions).
}
\details{
GitHub/URL sources are read from the code text, not the package name: a call
\code{remotes::install_github("user/repo")} names the source \code{user/repo}, which is
matched back to the package by the repo's basename. When a script pins a ref
(\code{user/repo@v1.2}), the ref is kept.
}
\examples{
code_text <- c(
  "library(dplyr)",
  "remotes::install_github('tidyverse/ggplot2@v3.4.0')",
  "x <- stats::sd(1:10)"
)
repro_dependencies(code_text)
}
