% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_check_helpers.R
\name{data_check_scale_values}
\alias{data_check_scale_values}
\title{Flag values that fall outside a rating scale's valid range}
\usage{
data_check_scale_values(
  x,
  sentinels = .data_missing_sentinels,
  declared = NULL,
  valid_values = NULL,
  valid_range = NULL,
  n_max = 10
)
}
\arguments{
\item{x}{a numeric vector}

\item{sentinels}{candidate missing-data sentinel codes}

\item{declared}{optional codebook-declared missing codes (ground truth)}

\item{valid_values}{optional enumerated valid codes (ground truth)}

\item{valid_range}{optional \code{c(lo, hi)} valid range (ground truth)}

\item{n_max}{max number of values to list in the message}
}
\value{
list(problem, message, values, lower, upper, classes) where \code{classes}
labels each flagged value "missing", "typo:\if{html}{\out{<intended>}}", or "unexplained"
}
\description{
A rating scale (Likert / rating item) has a small set of consecutive valid
integer levels. Any value outside that set is a data problem, and this check
both flags it and, for each value, offers the most likely explanation:
\itemize{
\item a \strong{missing-data code} left as a number (a \code{-99} / \code{999} in the
sentinel list, or a codebook-\strong{declared} missing code) — recode to \code{NA};
\item a \strong{keying typo} of an in-scale value (a \code{33} for \code{3}, a \code{55} for
\code{5}) — the probable intended value is named;
\item otherwise an \strong{unexplained} out-of-range value to review.
}
The valid range is ground truth when \code{valid_values} / \code{valid_range} are
supplied (e.g. from a codebook), otherwise inferred by \code{.detect_likert_scale}.
A column that is not a rating scale (continuous, many-level, non-integer, too
few rows) has no fixed range and is not flagged here — unbounded variables
(age, reaction time) have no principled "valid range" to violate.
}
\details{
This unifies the former \code{data_check_out_of_range} and
\code{data_check_miscoded_missing}: one detector run, one finding per column.
}
\keyword{internal}
