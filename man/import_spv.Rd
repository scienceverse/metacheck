% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spv.R
\name{import_spv}
\alias{import_spv}
\title{Read the statistical result tables from an SPSS Viewer (.spv) file}
\usage{
import_spv(path)
}
\arguments{
\item{path}{path to a \code{.spv} file}
}
\value{
a list of result tables, each \code{list(analysis, title, data, syntax, table_index)} — the same shape \code{\link[=read_stat_tables]{read_stat_tables()}} returns for
\code{.jasp}/\code{.omv} (\code{syntax} is \code{.spv}-specific: the exact SPSS syntax that
produced the table, when recoverable). Empty list if the archive has no
structure XML or no tables decode.
}
\description{
Ties the structure reader (\code{\link[=.spv_read_structure]{.spv_read_structure()}}, which table exists,
what analysis produced it, which format its data is in) and the two
decoders (\code{\link[=.spv_decode_light_table]{.spv_decode_light_table()}} for modern tables,
\code{\link[=.spv_decode_legacy_data]{.spv_decode_legacy_data()}} + \code{\link[=.spv_decode_legacy_table]{.spv_decode_legacy_table()}} for pre-21
tables) into the SAME shape \code{\link[=read_stat_tables]{read_stat_tables()}} already returns from
JASP's \code{analyses.json} and jamovi's protobuf blobs.
}
\details{
A table that fails to decode (an unsupported construct in either format,
or real malformation) is skipped rather than aborting the whole file, so
one bad table does not lose every other result in the same archive.
}
