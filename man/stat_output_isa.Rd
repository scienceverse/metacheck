% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stat-output.R
\name{stat_output_isa}
\alias{stat_output_isa}
\title{Serialise extracted result tables as a validated ISA-JSON document}
\usage{
stat_output_isa(tables, paper_id = "metacheck", source_file = NA_character_)
}
\arguments{
\item{tables}{the list returned by \code{\link[=read_stat_tables]{read_stat_tables()}}}

\item{paper_id}{paper id / DOI, used in identifiers and the study title}

\item{source_file}{basename of the originating \code{.jasp}/\code{.omv}, recorded as the
assay's data file and technology platform}
}
\value{
a list (ISA Investigation) ready to serialise with
\code{jsonlite::toJSON(auto_unbox = TRUE)}. \code{NULL} when there are no tables.
}
\description{
Builds a complete ISA-JSON Investigation from the result tables of ONE
\code{.jasp}/\code{.omv} file, following the modelling validated for metacheck: one file
= one dataset = one Sample; each analysis heading is a Protocol/Process on that
shared Sample; each result row is a Material whose \code{characteristics} are the
STATO-typed statistic values (via \code{\link[=stato_type_column]{stato_type_column()}}). The document
validates against the bundled ISA v1.0 schemas (\verb{inst/schema/isa-json/}).
}
