% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/archive-dataverse.R
\name{dataverse_file_download}
\alias{dataverse_file_download}
\title{Download all files from a Dataverse dataset}
\usage{
dataverse_file_download(
  host,
  doi,
  download_to = ".",
  max_file_size = 10,
  max_download_size = 100,
  unzip_types = NULL,
  pb = NULL
)
}
\arguments{
\item{host}{the Dataverse installation's hostname (e.g. "dataverse.harvard.edu")}

\item{doi}{the dataset's persistentId (a DOI, e.g. "10.7910/DVN/GGDUND")}

\item{download_to}{path to download to}

\item{max_file_size}{maximum file size to download (in MB) - set to NULL or
Inf for no restrictions}

\item{max_download_size}{maximum total size to download - set to NULL or
Inf for no restrictions}

\item{unzip_types}{file categories to extract from \code{.zip} files in the
dataset rather than downloading the zip whole, named as
\code{\link[=data_classify_files]{data_classify_files()}} names them: \code{"data"}, \code{"code"}, \code{"materials"},
\code{"documentation"}, \code{"output"}, \code{"unknown"}. More than one may be given.
\code{NULL} (the default) downloads zips whole and leaves them zipped, as
before. \code{max_file_size} applies to each extracted member rather than to
the archive.}

\item{pb}{a progress bar passed from another function}
}
\value{
data frame of file info. When members are extracted from a zip, its
row reports the zip with \code{extracted} giving the number of members written;
\code{size_on_disk} and \code{checksum_ok} are then \code{NA}, because what is on disk is
the members rather than the archive Dataverse published a size and MD5 for.
}
\description{
Creates a directory for the dataset and downloads all of its files. Returns
(invisibly) a data frame with file info.
}
\details{
You can limit downloads to only files under a specific size (defaults to
10MB) and only a maximum download size (largest files will be omitted until
total size is under the limit). Omitted files will be listed as messages in
verbose mode, and included in the returned data frame with the \code{downloaded}
column value set to FALSE.

A \code{.zip} in the dataset is normally fetched whole and left as a zip. Set
\code{unzip_types} to pull only the files you want out of it instead: Dataverse's
file-download endpoint honours byte-range requests (verified 2026-08-14
against demo.dataverse.org: \code{HEAD}/\code{Range} both return \code{Accept-Ranges: bytes}
and a \verb{206 Partial Content} response) and each member of a zip is compressed
separately, so the wanted members can be read out of the archive while the
rest is never transferred -- the same mechanism \code{\link[=zenodo_file_download]{zenodo_file_download()}}
uses. When the archive cannot be read that way, it is downloaded whole as
before, so this never loses a file -- it only avoids transferring one.
}
\examples{
\dontrun{
  dataverse_file_download("dataverse.harvard.edu", "10.7910/DVN/GGDUND")

  # take only the data files out of any zip in the dataset
  dataverse_file_download("dataverse.harvard.edu", "10.7910/DVN/GGDUND",
                          unzip_types = "data")
}
}
