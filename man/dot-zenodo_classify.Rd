% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/archive-zenodo-upload.R
\name{.zenodo_classify}
\alias{.zenodo_classify}
\title{Classify files the way the check modules do}
\usage{
.zenodo_classify(files)
}
\arguments{
\item{files}{full paths to the files}
}
\value{
a character vector of categories, one per file
}
\description{
\code{repo_check()}, \code{code_check()} and \code{data_check()} all classify a
repository's files with \code{data_classify_files()}, into six categories:
\code{data}, \code{code}, \code{materials}, \code{documentation}, \code{output}, and \code{unknown}.
Splitting an upload calls the very same function, so a file cannot end up in
one category when it is checked and another when it is archived.
}
\details{
That function reads names and paths only, never file contents, in three
steps: a format-locked extension (a \code{.sav} is code, a \code{.csv} is data), then
a category word used as a whole token in the path (\verb{Materials/},
\code{results.docx}), then a crosswalk through the coarse file type, where
\code{image}, \code{audio}, \code{video}, \verb{3D}, \code{exec}, \code{config} and \code{font} all become
\code{materials}. Because nothing is opened, classifying costs no time even for a
folder of very large files.
}
\keyword{internal}
