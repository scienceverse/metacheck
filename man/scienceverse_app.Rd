% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app.R
\name{scienceverse_app}
\alias{scienceverse_app}
\title{Launch the scienceverse archive browser}
\usage{
scienceverse_app(db_path = .sv_default_db(), quiet = FALSE, ...)
}
\arguments{
\item{db_path}{path to the SQLite archive. Defaults to
\code{getOption("metacheck.scienceverse.db")} when set, else the canonical
archive \code{\link[=add_to_scienceverse]{add_to_scienceverse()}} writes to. Set the option once (e.g. in
\code{.Rprofile}) to always launch against a chosen archive without passing
\code{db_path} every time.}

\item{quiet}{whether to show debugging messages in the console.}

\item{...}{arguments passed to \code{\link[shiny:runApp]{shiny::runApp()}}.}
}
\value{
NULL (invisibly)
}
\description{
Launch a Shiny app that searches a scienceverse SQLite archive built by
\code{\link[=add_to_scienceverse]{add_to_scienceverse()}}. Unlike \code{\link[=trove_app]{trove_app()}} (which indexes a folder tree in
memory on every launch), this queries the database live, so it starts
instantly and can also search the extracted findings (statistics, code
metrics, data/Excel issues) and each paper's full text — surfaces the folder
index does not expose.
}
\details{
Four tabs: Papers (metadata + full-text search, with per-paper studies,
checks and files), Findings (free-text plus numeric filters such as F greater
than 5 or p below .05), Scales & Tasks, and Files (the download manifest).
Text boxes use the same Google-style \code{field:value} grammar as \code{\link[=trove_app]{trove_app()}}.
}
\examples{
\dontrun{
scienceverse_app()                       # the default archive
scienceverse_app("my_archive.sqlite")    # a specific one

# or set once per session / in .Rprofile:
options(metacheck.scienceverse.db = "C:/path/to/scienceverse.sqlite")
scienceverse_app()
}

}
