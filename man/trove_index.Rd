% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trove-index.R
\name{trove_index}
\alias{trove_index}
\title{Build a searchable trove index from a corpus root}
\usage{
trove_index(root = ".", max_depth = 4L, quiet = FALSE)
}
\arguments{
\item{root}{directory containing one or more metacheck collection roots
(folders with a \code{collection.json}). Defaults to the current directory.}

\item{max_depth}{passed to \code{\link[=trove_find_collections]{trove_find_collections()}}}

\item{quiet}{whether to suppress the progress message}
}
\value{
a list of class \code{trove_index} with \code{papers}, \code{studies}, \code{variables},
\code{scales} data frames plus a \code{root} attribute
}
\description{
Discover every metacheck collection under \code{root} and flatten them into four
tidy data frames: \code{papers}, \code{studies}, \code{variables}, and \code{scales}. This is the
backing store the trove app searches.
}
\examples{
\dontrun{
idx <- trove_index(".")
idx$papers
}
}
