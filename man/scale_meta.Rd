% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scale_items.R
\docType{data}
\name{scale_meta}
\alias{scale_meta}
\title{Psychometric Scale Metadata (item-level)}
\format{
A data frame with one row per instrument and 13 columns:
\describe{
\item{code}{OpenScales code; joins to \code{scales$code}, \code{scale_items$code} and
\code{scale_scoring$code}.}
\item{name}{Canonical instrument name.}
\item{acronym}{Abbreviation; empty when upstream supplies only a catalogue
ID (\code{PX010201}, \code{MISS10991}) rather than a real acronym.}
\item{collection}{\code{"ipip"}, \code{"phenx"}, \code{"openscales"} or \code{"miss"}.}
\item{license}{Upstream licence string (e.g. \code{"Public Domain"},
\code{"CC BY 4.0"}).}
\item{citation}{Source publication, when recorded.}
\item{url}{Upstream reference URL.}
\item{domain}{Broad content domain (e.g. \code{"Personality"}).}
\item{n_items}{Number of items with resolvable English wording.}
\item{n_reverse}{Number of reverse-keyed items.}
\item{languages}{Comma-separated translation codes available upstream.}
\item{text_ok}{\code{TRUE} when the name is safe to regex against manuscript
prose — i.e. it names an \emph{instrument} rather than a \emph{topic}. \code{FALSE} for
\strong{all} PhenX entries, which are named after the construct measured
(\code{"Insomnia"}, \code{"General Well-being"}) rather than the instrument;
matching those against running text would fire on ordinary methods
prose. Other collections name real instruments, so short names there
(\code{"Grit Scale"}, \code{"COPE Inventory"}) still qualify. Items remain
available for every row; only name matching is withheld.}
}
}
\source{
OpenScales (\url{https://github.com/stmueller/OpenScales}).
Rebuild with \code{data-raw/scale_items.R}.
}
\usage{
scale_meta
}
\description{
One row per instrument harvested from the OpenScales \code{.osd} definition files,
the item-level companion to \link{scales}. Where \code{scales} answers \emph{"is this column
block the PANAS?"} from a name or acronym, this table plus \link{scale_items} and
\link{scale_scoring} describe what the instrument actually contains: its items,
subscales, reverse-keyed items and reliabilities.
}
\details{
Built from the OpenScales repository rather than its \code{manifest.json}, which
is metadata-only and covers ~200 of the ~1,100 available definitions.
Non-commercial / no-derivatives scales (the upstream \verb{restricted/}
collection) are excluded so the data can ship under the package licence.
}
\seealso{
\link{scales} for name/acronym identification, \link{scale_items} for item
wording, \link{scale_scoring} for subscale definitions.
}
\keyword{internal}
