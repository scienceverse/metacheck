% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/repo-download.R
\name{download_repo_files}
\alias{download_repo_files}
\title{Download the files listed by \code{repo_check()}}
\usage{
download_repo_files(
  files,
  max_file_size = 100,
  max_download_size = 500,
  zip_timeout_s = 120,
  cache = FALSE,
  skip_on_api_limit = FALSE,
  pb = NULL
)
}
\arguments{
\item{files}{a data.frame of files (as returned by \code{repo_check()}'s
\code{table}): needs \code{repo_url}, \code{file_url}, and \code{file_path} or \code{file_name}}

\item{max_file_size}{largest single file to download, in MB}

\item{max_download_size}{largest total cached footprint per repository, in MB}

\item{zip_timeout_s}{timeout (seconds) for a whole-repo zip download attempt
before falling back to file-by-file fetching}

\item{cache}{if \code{TRUE}, write into the persistent rappdirs cache (survives
across sessions, never cleared automatically — see \code{repo_cache_clear()});
if \code{FALSE} (default), write into a per-session temp directory that R
removes on exit}

\item{skip_on_api_limit}{if \code{FALSE} (the default), a download that hits a
host-confirmed exhausted rate limit (a 429 whose response carries
\code{RateLimit-Remaining: 0}/\code{X-RateLimit-Remaining: 0} and a parseable
reset time — currently known to apply to Dryad, Zenodo, GitHub, and
GitLab; other hosts do not expose this and are unaffected either way)
waits for the host's own stated reset before retrying, however long that
is — appropriate for an unattended corpus build, where completeness
matters more than wall-clock time. Set \code{TRUE} to give up on that file
immediately instead: it is recorded in the \code{"failed"} attribute with an
\code{"API rate limit exhausted: "}-prefixed message, filterable from every
other kind of failure, so a caller can re-run later (once the quota has
reset) targeting only those specific files rather than waiting inline.
An ordinary transient refusal (a 429 without a confirmed exhausted-quota
signal, a 403/5xx) still retries with ordinary backoff regardless of
this setting — it only changes what happens once a real quota
exhaustion is confirmed.}

\item{pb}{an optional progress bar object (see \code{pb()}), ticked as files
download}
}
\value{
\code{files} with \code{file_location} filled in for every file retrieved
(unchanged, i.e. \code{NA}, for files that were skipped or failed), and three
attributes: \code{"gated"} (data.frame: \code{repo_url}, \code{message} — repositories
refused outright by the size caps), \code{"oversize_skipped"} (data.frame:
\code{repo_url}, \code{file_name}, \code{file_size} — individual files skipped under
\code{max_file_size}), and \code{"failed"} (data.frame: \code{repo_url}, \code{file_name},
\code{error} — files whose download was attempted but errored, e.g. a
transient network failure, or — when \code{skip_on_api_limit = TRUE} and the
host confirmed it — an exhausted API rate limit; see \code{skip_on_api_limit}).
}
\description{
Fetches the bytes for a table of repository files (as produced by
\code{repo_check()}), writing each into a per-session temp directory or a
persistent on-disk cache, and fills in \code{file_location} for every file it
successfully retrieves. Where a whole-repo archive download is available
(OSF's Waterbutler \verb{?zip=} endpoint, Zenodo's \code{files-archive} endpoint,
Dataverse's \verb{/api/access/dataset} endpoint, Dryad's \code{stash:download}
endpoint, a GitHub zipball), it is used instead of one HTTP request per
file; a repo whose archive download fails, or is rejected by the
size/worth-it gate, falls back to file-by-file fetching automatically.
Figshare, 4TU.ResearchData (Figshare-compatible), and ReShare have no
documented whole-record bulk endpoint, so their files are always fetched
one by one.
}
\details{
Two independent size gates apply: \code{max_file_size} skips oversize files
individually (the rest of the repository still downloads); \code{max_download_size}
is a budget on the repository's TOTAL cached footprint (already-cached plus
newly-downloaded), not on a single run's increment — already-cached files are
kept for free, then the smallest still-missing files are added until the next
one would exceed the budget, so re-running never grows a repository's cache
past the budget and the canonical "files we ever download" set is fixed.
}
\keyword{internal}
