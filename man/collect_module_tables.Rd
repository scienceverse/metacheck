% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/archive-report.R
\name{collect_module_tables}
\alias{collect_module_tables}
\title{Stack one module's full tables across every saved paper}
\usage{
collect_module_tables(results_dir, module, element = "table")
}
\arguments{
\item{results_dir}{directory holding the per-paper \verb{*.rds} files.}

\item{module}{the module name to extract (e.g. \code{"data_validate"}).}

\item{element}{which list element of that module to bind (default \code{"table"}).}
}
\value{
a data.frame of the stacked element across papers (empty if none had
it), with a leading \code{paper_id} column.
}
\description{
Reads every \verb{<paper_id>.rds} written by \code{\link[=capture_module_tables]{capture_module_tables()}} and binds a
chosen element (default \code{"table"}; also e.g. \code{"summary_table"}, or a module's
extra frame like \code{"careless"}) of ONE module across all papers into a single
data frame, tagged with \code{paper_id}. This is the corpus-level view of a
module's real output, with full fidelity — no truncation or type coercion.
}
\examples{
\dontrun{
collect_module_tables("output/collabra/_tables", "data_validate")
collect_module_tables("output/collabra/_tables", "codebook_check",
                      element = "summary_table")
}
}
\seealso{
\code{\link[=capture_module_tables]{capture_module_tables()}}
}
