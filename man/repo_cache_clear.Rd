% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/repo-download.R
\name{repo_cache_clear}
\alias{repo_cache_clear}
\title{Delete the downloaded-repository file cache}
\usage{
repo_cache_clear(repo_url = NULL, quiet = FALSE)
}
\arguments{
\item{repo_url}{optional character vector of repository URLs. When supplied,
only those repositories' cached files are deleted; otherwise the entire
cache is cleared.}

\item{quiet}{if \code{FALSE} (default), report how much was freed.}
}
\value{
the number of bytes freed (numeric), invisibly.
}
\description{
Removes files fetched from data repositories by \code{data_check} / \code{code_check}
from the on-disk cache (see \code{\link[=repo_cache_dir]{repo_cache_dir()}}). The cache only speeds up
re-runs — anything deleted is simply re-downloaded when next needed — so
clearing it is always safe; the usual reason is to reclaim disk space after a
large corpus build.
}
\details{
By default the whole cache is removed. Pass \code{repo_url} to remove only the
cached files of specific repositories.
}
\examples{
\dontrun{
# clear everything
repo_cache_clear()

# clear one repository only
repo_cache_clear("https://osf.io/abcde")
}
}
\seealso{
\code{\link[=repo_cache_dir]{repo_cache_dir()}}, \code{\link[=repo_cache_size]{repo_cache_size()}}
}
