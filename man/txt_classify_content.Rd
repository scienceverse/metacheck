% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_check_helpers.R
\name{txt_classify_content}
\alias{txt_classify_content}
\title{Classify a downloaded .txt file from its content}
\usage{
txt_classify_content(path)
}
\arguments{
\item{path}{path to a local \code{.txt} file.}
}
\value{
a single data_check type, or \code{NA_character_} when undecided.
}
\description{
A \code{.txt} extension says nothing about what a file holds: research repositories
ship experiment data (E-Prime exports, Ibex/task logs), codebooks, and plain
prose notes all as \code{.txt}. The name-based classifier
(\code{\link[=data_classify_files]{data_classify_files()}}) runs on remote file names \emph{before} download, so it
cannot tell these apart and settles on a conservative guess. Once the file is
on disk, its content can. This is the same fetch-then-reclassify pattern the
module already uses for zips.
}
\details{
Returns the data_check type the content implies:
\itemize{
\item \code{"data"} for an E-Prime export (its fixed \verb{*** Header Start ***} /
\verb{LevelName:} header), or for a delimited table with a real header row;
\item \code{NA_character_} when the content is not recognised, meaning "keep whatever
the name-based classifier decided" — never a downgrade on a guess.
}

The file is only read, never modified or removed: the download cache is
persistent by design (see \code{\link[=repo_cache_dir]{repo_cache_dir()}}), so classification decides how a
file is \emph{used}, not whether it survives.
}
\keyword{internal}
