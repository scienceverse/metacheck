% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reproducibility_check_docker.R
\name{repro_install_deps_docker}
\alias{repro_install_deps_docker}
\title{Install R dependencies inside a throwaway Docker container}
\usage{
repro_install_deps_docker(
  install_deps,
  lib_dir,
  image = "rocker/r-ver:latest",
  timeout = 600
)
}
\arguments{
\item{install_deps}{a data frame as produced inside \code{reproducibility_check()}
(\code{package}, \code{source}, \code{ref}, \code{base}) — the non-base rows of
\code{\link[=repro_dependencies]{repro_dependencies()}}'s output}

\item{lib_dir}{throwaway library directory ON THE HOST (created if absent);
bind-mounted into the container at \verb{/rlib}}

\item{image}{the Docker image to install into (see \code{\link[=.repro_docker_image_for]{.repro_docker_image_for()}})}

\item{timeout}{per-package-batch timeout in seconds for the whole install
container run (default 600 — installs can be slow, esp. compiling from
source)}
}
\value{
a data frame with \code{package}, \code{source}, \code{installed} (logical),
\code{message} (error text on failure, else ""), \code{via_archive} (always FALSE
— the CRAN Archive retry \code{\link[=repro_install_deps]{repro_install_deps()}} does is not duplicated
here; a package unavailable on live CRAN is simply recorded as failed) —
same columns as \code{\link[=repro_install_deps]{repro_install_deps()}} so callers do not need to branch
on which backend ran.
}
\description{
The Docker-backed twin of \code{\link[=repro_install_deps]{repro_install_deps()}}: same return contract,
same throwaway-library semantics, but the actual \code{install.packages()}/
\code{remotes::install_github()} call runs INSIDE a container (default
networking, since CRAN/GitHub must be reachable), writing into \code{lib_dir}
via a bind mount — so the run phase (\code{\link[=repro_run_scripts_docker]{repro_run_scripts_docker()}}), which
mounts the same \code{lib_dir} with the network off, finds the packages already
there.
}
\details{
Unlike \code{\link[=repro_install_deps]{repro_install_deps()}}, there is no \code{cran_to_main_lib} option: a
container has no access to (and must not be given access to) the host's
real R library, so every install always goes into the throwaway \code{lib_dir}.
}
