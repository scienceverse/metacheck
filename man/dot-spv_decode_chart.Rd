% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spv.R
\name{.spv_decode_chart}
\alias{.spv_decode_chart}
\title{Decode one .spv chart (VizML chart.xml + its own raw case data)}
\usage{
.spv_decode_chart(xml_raw, data, title = NA_character_)
}
\arguments{
\item{xml_raw}{raw vector or string: the \code{path} member's XML content (a
\verb{<visualization>} document, the same VizML dialect a legacy table's
detail-xml uses).}

\item{data}{the decoded raw case data for this chart's \code{dataPath} member,
as returned by \code{\link[=.spv_decode_legacy_data]{.spv_decode_legacy_data()}} -- used for \verb{<point>}/
\verb{<interval>} charts, and as a \verb{<schema>} box plot's fallback data source
when it has no \verb{<embeddedSource>}.}

\item{title}{optional chart title (from the structure reader's own
context), used when the XML's own \verb{<labelFrame>} title cannot be read.}
}
\value{
For a \verb{<point>}/\verb{<interval>} chart: a data.frame with columns \code{x},
\code{y} (one row per case with both values present). For a \verb{<schema>} box
plot: a data.frame with columns \code{category} (the relabelled group name)
and \code{value} (one row per case). \code{NULL} if this chart uses a construct
out of this port's scope, or its variable/data references cannot be
resolved. Every shape carries attributes \code{spv_chart_title},
\code{spv_chart_xlab}, \code{spv_chart_ylab} (character), and \code{spv_chart_type}
(\code{"point"}, \code{"interval"}, or \code{"boxplot"}); a \verb{<point>} chart also
carries \code{spv_chart_fits} (a list of \code{.spvviz_function_guide()} results,
possibly empty) for the fitted trend lines SPSS itself already computed.
}
\description{
Handles three mark types, found by inspecting real chart-bearing files
from Zenodo/Figshare rather than any spec (\code{.spv} charts have none -- see
the section header above): a \verb{<point>} scatter mark, an \verb{<interval>}
histogram/bar mark (both resolved from \code{.spv_decode_legacy_data()}'s case
data via \code{.spvviz_decode_xy()}), and a \verb{<schema>} box-plot mark (its data
resolved from EITHER an inline \verb{<embeddedSource>} element or, when that
is absent, the same case data the other two marks use -- see
\code{.spvviz_decode_boxplot_source()} / \code{.spvviz_decode_boxplot_databin()}).
Any other mark (\verb{<line>}/\verb{<bar>}/\verb{<area>}/...) is out of this version's
scope.
}
\keyword{internal}
