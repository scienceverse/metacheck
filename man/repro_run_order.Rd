% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reproducibility_check.R
\name{repro_run_order}
\alias{repro_run_order}
\title{Determine the order code files must run in}
\usage{
repro_run_order(files, extra_edges = NULL)
}
\arguments{
\item{files}{a data frame with one row per code file, columns \code{file_name},
and \code{reads} / \code{writes} / \code{sources} list-columns of basenames (as produced
by \code{\link[=repro_file_io]{repro_file_io()}}). When \code{reads}/\code{writes}/\code{sources} are absent they are
derived from a \code{code_text} list-column if present.}
}
\value{
a data frame with columns \code{file_name}, \code{order} (1-based run
position, NA when unplaceable), \code{depends_on} (comma-joined file_names that
must run earlier), and \code{order_basis} (\code{dependency}, \code{numeric}, or \code{none}).
Attribute \code{"cycle"} is a character vector of file_names in a dependency
cycle (empty when acyclic); attribute \code{"ambiguous"} is TRUE when no ordering
signal placed a multi-file set (they share \code{order = NA}, basis \code{none}).
}
\description{
Scripts in a repository usually depend on each other: a preparation script
writes a file a later analysis script reads, or one \code{source()}s another.
Running them in the wrong order fails for reasons that have nothing to do with
reproducibility, so a run must order them first. This derives an order from
three signals, in decreasing strength:
}
\details{
\enumerate{
\item \strong{read-after-write} — if file A's code writes \code{x.csv} and file B's code
reads \code{x.csv}, A must precede B (hard data dependency);
\item \strong{\code{source()} edges} — if A \code{source()}s B, B must precede A;
\item \strong{numeric filename prefixes} — \verb{0_prep.R} before \verb{1_analysis.R} (a weak
tie-breaker, not a dependency).
}

The read/write and source references come from the shared static analysis
(\code{\link[=code_file_refs]{code_file_refs()}} and a source scan); matching is by basename (as
elsewhere). The dependency graph is topologically sorted; a cycle (A reads a
file B writes and vice versa) cannot be ordered and is reported.
}
\examples{
files <- data.frame(file_name = c("1_analysis.R", "0_prep.R"))
files$reads   <- list("clean.csv", character(0))
files$writes  <- list(character(0), "clean.csv")
files$sources <- list(character(0), character(0))
repro_run_order(files)
}
