% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stat-output.R
\name{stat_output_write}
\alias{stat_output_write}
\title{Write extracted statistical output to a dedicated folder}
\usage{
stat_output_write(stat_output, root)
}
\arguments{
\item{stat_output}{a list as accumulated by \code{reproducibility_check} — each
element a list with \code{file} (source file name), \code{isa}
(\code{\link[=stat_output_isa]{stat_output_isa()}}'s document), and \code{long} (\code{\link[=stat_results_long]{stat_results_long()}}'s
table)}

\item{root}{the materialised layout root (the same directory \verb{data/} is
copied into); \verb{statistical_output/} is created under it}
}
\value{
the path to \verb{statistical_output/}, invisibly, or \code{NULL} (nothing
written) when \code{stat_output} is empty or carries no rows/documents.
}
\description{
Writes \code{reproducibility_check}'s accumulated \code{stat_output} (one element per
source file: JASP/jamovi files via \code{\link[=read_stat_tables]{read_stat_tables()}}, executed R scripts
via \code{\link[=read_r_output]{read_r_output()}}) to \verb{<root>/statistical_output/}, a folder sibling to
the materialised \verb{data/} — so the extracted statistics sit alongside the data
and code that produced them. Two views are written from the SAME extraction:
one combined \code{results_long.csv} (every source file's \code{\link[=stat_results_long]{stat_results_long()}}
rows stacked, one row per extracted statistic, easiest to load and filter),
and one \verb{<codename>.statistical_output.json} per source file (its
\code{\link[=stat_output_isa]{stat_output_isa()}} document). Called from inside \code{reproducibility_check}
itself (not from the later psychds/scienceverse conversion step), so the
folder exists at the same point in the pipeline the data/code files do.
}
