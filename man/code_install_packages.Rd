% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/code_check.R
\name{code_install_packages}
\alias{code_install_packages}
\title{Find install.packages() calls in code}
\usage{
code_install_packages(code_text)
}
\arguments{
\item{code_text}{the code text for a single file (character vector), ideally
comment-free (as produced by \code{\link[=code_remove_comments]{code_remove_comments()}})}
}
\value{
a data frame with columns \code{install_packages_call} (the
\code{install.packages(...)} text as written) and \code{line} (its line number).
Empty frame when none are found.
}
\description{
An \code{install.packages()} call in analysis code installs software on
whoever runs the script, without asking -- a portability and security
concern raised in issue #398: at minimum it is disruptive (unexpected
installs, network access, a package version the author didn't test
against), and at worst a security risk if it points at a private/
untrusted repository. Best practice is to document required packages
(a README, a \code{renv.lock}/\code{DESCRIPTION}) rather than install them from
inside the analysis code itself. This scans the (comment-free) code for
\code{install.packages(...)} calls and returns one row per call, with the
argument as written. Only a LIVE call is ever seen here: a commented-out
\verb{#install.packages(...)} is invisible to this function by construction,
the same way \code{\link[=code_setwd]{code_setwd()}} never sees a commented-out \code{setwd()} --
both are only ever called on \code{code_remove_comments()}'s output. R-only
(the construct is R's; other languages have their own package managers,
which this does not scan).
}
\examples{
code_text <- c(
  "install.packages('ggplot2')",
  "library(ggplot2)"
)
code_install_packages(code_text)
}
