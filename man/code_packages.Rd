% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/code_check.R
\name{code_packages}
\alias{code_packages}
\title{Distinct packages from a code_check table}
\usage{
code_packages(packages)
}
\arguments{
\item{packages}{a character vector of comma-joined package strings (e.g.
\code{code_check(...)$table$packages}), or a \code{code_check} table (data frame with
a \code{packages} column).}
}
\value{
a sorted character vector of distinct package names (possibly empty)
}
\description{
The \code{code_check} module stores the packages each code file loads as a
comma-joined string in its \code{table$packages} column. This returns the sorted,
de-duplicated union across a set of those rows — the paper-level dependency
list used for the module summary, the manifest \code{code} section, and the
\code{requirements.txt} written into a Psych-DS archive by \code{convert_psychds()}.
}
\examples{
code_packages(c("dplyr, ggplot2", "", "dplyr, tidyr"))
}
