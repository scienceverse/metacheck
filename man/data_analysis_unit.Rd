% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_check_helpers.R
\name{data_analysis_unit}
\alias{data_analysis_unit}
\title{Infer the analysis unit (unit of observation) of a data file}
\usage{
data_analysis_unit(df, id_cols = NULL)
}
\arguments{
\item{df}{a data.frame (the read data file)}

\item{id_cols}{optional character vector of identifier column names (e.g. from
data_check's \code{role == "identifier"}); when NULL, inferred by name pattern}
}
\value{
a list with \code{unit} (one of the above or NA) and \code{reason} (a short
human-readable explanation).
}
\description{
Rule-based inference of what one row of a data frame represents — \code{"person"},
\code{"trial"}, \code{"session"}, \code{"dyad"}, or \code{NA} when unclear (DDI \code{analysisUnit}).
Uses the identifier column(s) and their uniqueness: a unique-per-row id with
no repeat structure is person-level (wide); a repeating id together with a
trial/stimulus column is trial-level (long); a repeating id with a
session/wave column is a repeated-measures session unit; two distinct id-like
columns suggest a dyad. Needs enough rows to judge repetition.
}
\keyword{internal}
