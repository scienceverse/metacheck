% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_check_helpers.R
\name{data_promote_header_row}
\alias{data_promote_header_row}
\title{Promote a mis-placed header row and drop leading metadata rows}
\usage{
data_promote_header_row(df, raw_rows = NULL, max_scan = 4L)
}
\arguments{
\item{df}{the data.frame as normally read (reader-assigned names)}

\item{raw_rows}{optional list of character vectors: the first rows of the file
read WITHOUT a header, so the reader-swallowed header row is visible. When
supplied it is the authoritative scan window.}

\item{max_scan}{how many leading rows to consider as metadata above the header}
}
\value{
a list with \code{df} (possibly re-headed), \code{promoted} (1-based count of
metadata rows removed above the header, 0 = unchanged), and \code{stripped}
(character vectors of those removed rows, for reporting).
}
\description{
When a banner / blank / units / repeated-label row sits ABOVE the real header,
the reader takes that top row as the header (inventing \verb{…N} names, or spreading
one label — \code{CDA} merged across 110 columns — into \verb{CDA…1 … CDA…110}). This
finds the true header among the first few rows via \code{\link[=.detect_header_row]{.detect_header_row()}},
promotes it to the column names, drops it and everything above, and re-types the
freed columns. It is the inverse of \code{\link[=data_strip_qualtrics_header]{data_strip_qualtrics_header()}} (which
strips junk rows BELOW a correct header).
}
\details{
The input must be read so that the real header is still a DATA row (i.e. read
headerless, or with the reader's own header as row-0 metadata). Pass the rows
via \code{raw_rows} (the file re-read headerless as character) so the detector can
see the row the reader swallowed as the header; without it the function falls
back to treating \code{df}'s own first rows as the scan window.
}
\keyword{internal}
