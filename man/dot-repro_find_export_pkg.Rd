% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reproducibility_check.R
\name{.repro_find_export_pkg}
\alias{.repro_find_export_pkg}
\title{Find the one installed package (if any) that unambiguously exports a name}
\usage{
.repro_find_export_pkg(name, candidates)
}
\arguments{
\item{name}{the undefined symbol (e.g. \code{"test_that"}, \code{"\%>\%"})}

\item{candidates}{character vector of candidate package names to check
(already scoped by the caller — see above)}
}
\value{
the single package name that exports \code{name}, or \code{NA_character_}
when zero or more than one candidate does (never guessed)
}
\description{
Used by the module's corrective re-run for a \verb{could not find function "X"}
error that no file in the paper's own code defines (the sibling case to
\code{\link[=repro_defined_vars]{repro_defined_vars()}}'s repo-file lookup, for when the missing symbol is a
PACKAGE export the author simply forgot to \code{library()}, not a local
variable/function). Deliberately scoped, not a blind search of every
installed or CRAN package: only \code{candidates} (the paper's own declared
dependencies plus \code{\link[=.repro_common_pkgs]{.repro_common_pkgs()}}'s small curated safe-list) are
checked, so an ambiguous common name (\verb{\%>\%}, \code{select}, ...) across the
package universe at large is never even considered — see this function's
caller for why a blind scan is unsafe (a real corpus check found \verb{\%>\%}
alone exported by 69 installed packages).
}
\details{
A candidate package that is not installed is silently skipped (not an
error): this only ever runs against packages already usable in the current
R installation, since \code{BiocManager}/\code{remotes}-style live lookups are out of
scope for a same-process check like this one.
}
\keyword{internal}
