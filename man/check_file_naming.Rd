% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/file-naming.R
\name{check_file_naming}
\alias{check_file_naming}
\title{Check repository files against the FAIR file-naming conventions}
\usage{
check_file_naming(file_name, file_path = file_name, data_type = NA_character_)
}
\arguments{
\item{file_name}{a character vector of file basenames}

\item{file_path}{a character vector of repo-relative paths (defaults to
\code{file_name} when not supplied — length-checks need the full path,
everything else only needs the basename)}

\item{data_type}{an optional character vector (same length as \code{file_name})
of \code{data_check} semantic types (see \code{data_classify_files()}), used only
for the classifiability rule}
}
\value{
a data.frame with one row per naming violation: \code{file_name},
\code{rule}, \code{severity} (\code{"bad"} or \code{"suggestion"} — see Details), \code{detail}.
0 rows when every file is clean.
}
\description{
Checks every file's name against the naming conventions from the metacheck
"machine readable FAIR data and code" guide: no spaces or special
characters, lowercase only, no CamelCase, an actually-classifiable name
(not \code{data_type == "unknown"}), zero-padded numbering across sibling files,
YYYYMMDD dates, and four path-length budgets (255/228/100/50 characters).
}
\details{
Rules split into two severities. \code{"bad"}: spaces, special characters,
diacritics, invalid YYYYMMDD dates, \code{data_type == "unknown"} (the file
could not be classified at all), and exceeding the hard 255-character path
limit — these break something real. \code{"suggestion"}: uppercase letters,
CamelCase, unpadded sibling numbering, and the softer 228/100/50
path-length budgets — real conventions worth following, but nothing in
metacheck's own classification or ordering logic actually depends on them
(classification matches a keyword anywhere in the name with any/no
separator — see \code{data_classify_files()}; nothing sorts files
lexicographically for correctness — see \code{repro_run_order()}'s numeric, not
string, tie-break). Underscore-vs-dash separator consistency is
deliberately NOT checked at all, for the same reason: metacheck's
classifiers never depend on it.
}
\keyword{internal}
