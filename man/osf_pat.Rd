% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/archive-osf-helpers.R
\name{osf_pat}
\alias{osf_pat}
\title{Set or get the OSF personal access token}
\usage{
osf_pat(pat = NULL)
}
\arguments{
\item{pat}{the token to set, or NULL to get the current token}
}
\value{
the current token (character; \code{""} when none is set)
}
\description{
Use \code{osf_pat()} to get the token used to authorise OSF API requests, or
\code{osf_pat("your-token")} to set it for the rest of the session.
}
\details{
Without a token you can make 100 API requests per hour; with one you can make
10,000 per day, so a token is worth setting before downloading whole projects
or listing everything a user has.

To create one, sign in at \url{https://osf.io/settings/tokens}, click "Create
token", give it a name, and tick the \code{osf.full_read} scope (add
\code{osf.full_write} only if you also intend to write to the OSF). Copy the token
immediately, as the OSF does not show it again.

The usual way to store it is a line in your \code{.Renviron} file (open it with
\code{usethis::edit_r_environ()}), which is read every time R starts:

\code{OSF_PAT="replace-with-your-token-string"}

Setting the token with \code{osf_pat()} overrides that for the current session
only, which is useful when you cannot edit \code{.Renviron} or want to use a
different account for one run.
}
\examples{
osf_pat() # returns "" unless a token is set
}
