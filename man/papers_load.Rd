% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import-papers.R
\name{papers_load}
\alias{papers_load}
\title{Load a paper corpus}
\usage{
papers_load(
  name,
  repo = "scienceverse/papers",
  cache = FALSE,
  overwrite = FALSE
)
}
\arguments{
\item{name}{name of the corpus, without the \code{.rds} extension
(see \code{\link[=papers_available]{papers_available()}})}

\item{repo}{GitHub repository in "owner/repo" format}

\item{cache}{whether to save the corpus to the user data directory for
reuse across sessions, instead of downloading it fresh each time}

\item{overwrite}{whether to re-download if already cached (only relevant
when \code{cache = TRUE})}
}
\value{
a paperlist object
}
\description{
Downloads a paper corpus RDS file from the scienceverse/papers GitHub
repository and loads it into R as a paperlist object. By default the file
is downloaded to a temporary location and discarded after loading, which
is the right choice for one-off use. Set \code{cache = TRUE} to save it
permanently in the user data directory instead, so subsequent calls reuse
the cached copy rather than re-downloading. Use \code{\link[=papers_remove]{papers_remove()}} to
delete a cached corpus.
}
\examples{
\dontrun{
collabra <- papers_load("collabra")
}
}
