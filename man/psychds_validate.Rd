% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/psychds-validate.R
\name{psychds_validate}
\alias{psychds_validate}
\title{Validate a Psych-DS dataset directory (native R)}
\usage{
psychds_validate(dir)
}
\arguments{
\item{dir}{path to the dataset directory (a study root containing
\code{dataset_description.json})}
}
\value{
a list with \code{valid} (TRUE when no error-severity issues), \code{issues}
(a list of issues), and \code{summary} (counts). Printed as a compact report.
}
\description{
Checks a directory against the Psych-DS 1.5 dataset standard: required
metadata file and fields, a \verb{data/} directory with \verb{*_data.csv} files, CSV
header/structure integrity, agreement between CSV columns and the
\code{variableMeasured} metadata, and data-file keyword naming. Returns the issues
found with their severities.
}
\details{
This is a native R reimplementation of the checks performed by the official
\href{https://github.com/psych-ds/psychds-validator/}{psychds-validator}; it does
not execute the schema's JavaScript rules or perform full schema.org JSON-LD
expansion (the schema.org-property warnings are therefore not reported).
}
\examples{
\dontrun{
res <- convert_psychds(paper, "psychds/mydata")
psychds_validate(res$output_dir)
}
}
