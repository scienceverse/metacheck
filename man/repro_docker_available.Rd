% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reproducibility_check_docker.R
\name{repro_docker_available}
\alias{repro_docker_available}
\title{Is Docker available and running?}
\usage{
repro_docker_available()
}
\value{
a list \code{ok} (logical) and \code{msg} (character, empty on success —
otherwise a human-readable reason, e.g. Docker not installed or the
daemon not running)
}
\description{
Checks \verb{docker info} succeeds, so a caller can fail early and clearly
(before materialising anything) rather than partway through a batch of
papers with a cryptic \verb{docker run} error. Used as the guard for
\code{sandbox = "docker"} the same way \code{requireNamespace("callr", ...)} guards
the process backend.
}
