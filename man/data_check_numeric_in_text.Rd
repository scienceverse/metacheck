% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_check_helpers.R
\name{data_check_numeric_in_text}
\alias{data_check_numeric_in_text}
\title{Flag a mostly-numeric column stored as text}
\usage{
data_check_numeric_in_text(x, threshold = 0.8, n_max = 10)
}
\arguments{
\item{x}{a character or factor vector}

\item{threshold}{minimum fraction of non-empty values that must parse as
numeric for the column to be considered "mostly numeric"}

\item{n_max}{max number of non-numeric values to list}
}
\value{
list(problem, message, values)
}
\description{
When a column read as character is mostly numbers but has a few values that
do not parse (e.g. "n/a", ">100", "50 approx"), those dirty cells forced the
whole column to text — a data-quality problem in the source, not a read
error. A \emph{fully} numeric text column is not flagged here: the file readers
auto-type clean numeric columns, so an all-numeric character column would
indicate a reader problem rather than a data problem.
}
\keyword{internal}
