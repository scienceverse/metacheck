% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_check_helpers.R
\name{parse_qsf}
\alias{parse_qsf}
\title{Parse a Qualtrics survey-definition file (.qsf) into codebook variables}
\usage{
parse_qsf(path)
}
\arguments{
\item{path}{path to a \code{.qsf} file}
}
\value{
a data.frame of variable definitions (\code{codebook_variable}, \code{label},
\code{codebook_source}, \code{group}, \code{value_labels}, \code{missing_values}, \code{question},
\code{universe}, \code{parse_method}), or \code{NULL} when the file is not a parseable QSF
or yields no questions.
}
\description{
Reads the survey object's questions and returns one row per data column the
export would produce, carrying the item wording and the coded response
options — the same shape as the other \code{parse_codebook()} back-ends, so the
rows join the codebook / scale pipeline unchanged. The \code{group} column holds
each question's \code{DataExportTag} stem, a high-confidence scale-block signal.
}
\details{
Column-name reconstruction: simple questions use the \code{DataExportTag}; matrix
and multi-answer questions expand to \verb{<DataExportTag>_<choiceCode>} (honouring
\code{ChoiceDataExportTags} when present). For a matrix (Likert) question the
\code{Choices} are the items (one column each) and the \code{Answers} are the shared
response scale points (the value labels); for single/multiple choice the
\code{Choices} are themselves the value labels.
}
\keyword{internal}
