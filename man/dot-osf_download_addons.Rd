% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/archive-osf-all.R
\name{.osf_download_addons}
\alias{.osf_download_addons}
\title{Download a node's files that live on a linked add-on}
\usage{
.osf_download_addons(node, node_dir, pb = NULL)
}
\arguments{
\item{node}{the OSF node ID}

\item{node_dir}{the folder this node's files are written to}

\item{pb}{a progress bar passed from another function}
}
\value{
a list with \code{files} and \code{bytes} retrieved
}
\description{
The OSF's \verb{?zip=} endpoint archives \code{osfstorage} and nothing else: asking it
for a \code{github} provider returns 404 (checked 2026-08-13). A project that
links an external repository therefore has files no archive can deliver --
29 of pngda's 57, for example -- so those are listed and fetched one at a
time. This is the only route to them, and it is why \code{mode = "all"} is not
purely archive-based.
}
\keyword{internal}
