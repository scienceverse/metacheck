% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_check_helpers.R
\name{data_is_manifest}
\alias{data_is_manifest}
\title{Detect a file manifest / table-of-contents masquerading as tabular data}
\usage{
data_is_manifest(df, repo_files, threshold = 0.8, min_exts = 2L)
}
\arguments{
\item{df}{a data.frame (the read tabular file)}

\item{repo_files}{a character vector of the other file names/paths in the
same repository (basenames are compared)}

\item{threshold}{minimum fraction of a column's non-empty values that must
resolve to repository files}

\item{min_exts}{minimum number of distinct referenced file extensions}
}
\value{
\code{TRUE} when \code{df} looks like a file manifest, else \code{FALSE}.
}
\description{
A manifest (e.g. a "table of contents" CSV) is structurally a valid tabular
file, so extension-based classification treats it as data. It is
distinguished from real research data by content, using the repository's own
file list as ground truth: a manifest has a column in which most values name
other files in the repository. This is name- and header-agnostic — it does
not rely on the file or its columns being \emph{called} anything in particular.
}
\details{
To avoid demoting genuine data that merely references assets (e.g. a
\code{stimulus} column of image filenames), a candidate column must both reach the
\code{threshold} of file references and reference at least \code{min_exts} distinct file
extensions (a manifest points across code/data/docs; an asset column is
usually one extension).
}
\keyword{internal}
