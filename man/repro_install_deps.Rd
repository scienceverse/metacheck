% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reproducibility_check.R
\name{repro_install_deps}
\alias{repro_install_deps}
\title{Install a paper's declared dependencies into a throwaway library}
\usage{
repro_install_deps(install_deps, lib_dir, cran_to_main_lib = FALSE)
}
\arguments{
\item{install_deps}{the module's \code{install_deps} frame (\code{package}, \code{source},
\code{ref}), base packages already excluded}

\item{lib_dir}{the throwaway library path (created if absent); ignored for
CRAN-source packages when \code{cran_to_main_lib = TRUE} (see below)}

\item{cran_to_main_lib}{if \code{TRUE}, CRAN-source packages are installed into
your DEFAULT library (no \code{lib} argument — same as calling
\code{install.packages(pkg)} yourself) instead of the throwaway \code{lib_dir}, and so
PERSIST after the run rather than being deleted with the sandbox. This
trades a one-time, permanent change to your R library for install work that
is shared across every later call needing the same package (useful when
\code{reproducibility_check()} runs across many papers in one session/script: a
CRAN package installed for an early paper is already present, and skipped
as already-installed, for every later one). GitHub/URL sources are
deliberately excluded from this and always go into the throwaway \code{lib_dir}
regardless of this argument — those are arbitrary, unreviewed code you
likely do not want permanently in your main library. Default \code{FALSE}
(everything throwaway, the original behaviour).

A CRAN-source package that \code{install.packages()} cannot find is not
necessarily a broken dependency: CRAN removes (archives) packages that stop
passing its checks, but every version ever published stays available as a
source tarball under the CRAN Archive
(\verb{https://cran.r-project.org/src/contrib/Archive/<pkg>/}). When the live-CRAN
install fails, this retries once from the Archive's most recent version
before giving up — a package the paper's authors could install at the time
(and that later dropped off live CRAN) still installs, instead of the run
recording an "errored" script for a cause that has nothing to do with the
paper's code. The retry is recorded in \code{via_archive} so the report can say
the run used a stale-but-real version rather than the one the authors had.}
}
\value{
a data frame with \code{package}, \code{source}, \code{installed} (logical),
\code{message} (error text on failure, else ""), and \code{via_archive} (logical,
TRUE when installed from the CRAN Archive after a live-CRAN failure).
}
\description{
Installs each non-base dependency into \code{lib_dir} (a temp library, never the
user's), so a run does not mutate the user's installed packages. CRAN packages
come from \code{install.packages()}; GitHub/URL sources from the ref the code named
(\code{remotes::install_github()} / \code{install.packages(url)}), honouring a pinned
\verb{@ref} when present. Every install's outcome is recorded; installing runs
package build/configure scripts, so this is part of the gated execute phase.
}
