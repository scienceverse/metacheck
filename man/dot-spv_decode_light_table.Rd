% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spv.R
\name{.spv_decode_light_table}
\alias{.spv_decode_light_table}
\title{Decode one light-binary SPV table member into a tidy data frame}
\usage{
.spv_decode_light_table(raw)
}
\arguments{
\item{raw}{raw vector: the exact bytes of the \code{.bin} zip member.}
}
\value{
a data.frame, or \code{NULL} if the member cannot be decoded (malformed
or an unsupported version) — callers should treat this as "try the next
fallback", not an error.
}
\description{
Reads the raw bytes of a \code{"<n>_lightTableData.bin"} zip member (the modern,
SPSS Statistics 21+ table-data format) and returns a rectangular table:
one row per data cell, one column per row/column/layer dimension (holding
that cell's category label) plus a \code{value} column (character, via
\code{.stat_num_to_chr()} for numeric cells so full precision survives). Layer
dimensions are included as columns too — filtering to the "current" layer,
if desired, is left to the caller, since some multi-layer tables are
genuinely all wanted (e.g. every group in a split-file analysis).
}
\keyword{internal}
