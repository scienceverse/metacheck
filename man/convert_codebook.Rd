% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/codebook-convert.R
\name{convert_codebook}
\alias{convert_codebook}
\title{Prepare a human-readable codebook for a repository}
\usage{
convert_codebook(
  paper,
  output_dir = NULL,
  render = TRUE,
  refresh_osf = FALSE,
  local_path = NULL,
  local_only = FALSE,
  model = llm_model(),
  params = list(),
  overwrite = FALSE
)
}
\arguments{
\item{paper}{a paper object (see \code{\link[=read]{read()}}), \strong{or} a captured result of
\code{report(paper, ...)} / \code{report_module_run(paper, ...)}. When a captured
result containing both \code{data_check} and \code{codebook_check} is passed, those
outputs are reused (with the paper recovered from the result) instead of
re-running; otherwise the modules are run.}

\item{output_dir}{directory to write the codebook inputs into; created if
needed. Defaults to \code{"codebook/<paper_id>"} under the working directory.}

\item{render}{whether to render \code{codebook.html} when the \code{codebook} package
and pandoc are available (\code{TRUE}, the default). When they are not, the
render is skipped regardless.}

\item{refresh_osf}{whether to fetch a fresh OSF file listing (see
\code{\link[=convert_psychds]{convert_psychds()}}); the default (\code{FALSE}) reuses the session's listing.}

\item{local_path, local_only}{passed to \code{data_check} when its output is not
already available (see \code{data_check()})}

\item{model, params}{passed to the underlying modules when \code{llm_use(TRUE)}}

\item{overwrite}{whether to overwrite an existing \code{output_dir}. When \code{FALSE}
(the default) and \code{output_dir} already exists, the function messages and
skips rather than erroring (the returned list has \code{existed = TRUE}).}
}
\value{
(invisibly) a list with \code{output_dir}, \code{n_studies}, \code{rds_files},
\code{rmd_files}, \code{metadata_files}, \code{html_files} (the paths written), and
\code{rendered} (whether any HTML was produced). When an existing \code{output_dir}
was skipped, the list additionally contains \code{existed = TRUE}.
}
\description{
Builds the inputs for a rich, human-readable codebook of a paper's data
repository and writes them to a side folder, so the codebook can be dropped
into the repository (or published) without changing the data itself. This is
the companion of \code{\link[=convert_psychds]{convert_psychds()}}: that function writes the \emph{machine}
readable Psych-DS dataset; this one writes a fully \strong{labelled} data frame
plus a ready-to-run R Markdown document for the
\href{https://github.com/rubenarslan/codebook}{codebook} package.
}
\details{
For each study the function assembles a data frame whose columns carry the
variable labels matched by \code{codebook_check()} (plus any value labels already
embedded in the data, e.g. from SPSS/Stata files), and whose dataset-level
\code{metadata} attribute holds the title, description, authors, DOI and keywords
from the paper. It writes:
\itemize{
\item \code{codebook_data.rds} — the labelled data frame (real data rows when a local
copy of the file is available, otherwise a zero-row frame carrying only the
attributes),
\item \code{codebook.Rmd} — a one-line-to-run template that loads the \code{.rds} and calls
\code{codebook::codebook()},
\item \code{codebook_metadata.json} — schema.org Dataset JSON-LD (written with base
tools, so it exists even without the \code{codebook} package).
}

When the \code{codebook} package \strong{and} pandoc are both installed, the \code{.Rmd} is
additionally rendered to \code{codebook.html}. Otherwise that step is skipped with
a message; the \code{.rds} + \code{.Rmd} let the user render it later. metacheck itself
does not depend on \code{codebook} or rmarkdown — they are only needed for the
optional HTML render.

Note this does not produce a Psych-DS \code{dataset_description.json}; the JSON-LD
here is discoverability metadata for the codebook, not a Psych-DS descriptor.
Use \code{\link[=convert_psychds]{convert_psychds()}} for a validation-passing dataset.
}
\examples{
\dontrun{
# Capture the checks, then build the codebook (modules reused, not re-run):
res <- report(paper, c("data_check", "codebook_check"))
convert_codebook(res)

# Or call directly on the paper:
convert_codebook(paper)

# then, if not auto-rendered:
rmarkdown::render("codebook/<paper_id>/codebook.Rmd")
}
}
\seealso{
\code{\link[=convert_psychds]{convert_psychds()}} for the machine-readable Psych-DS dataset.
}
