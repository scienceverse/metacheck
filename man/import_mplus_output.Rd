% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mplus.R
\name{import_mplus_output}
\alias{import_mplus_output}
\title{Read an Mplus (.out) output file}
\usage{
import_mplus_output(path)
}
\arguments{
\item{path}{path to a \code{.out} file}
}
\value{
a list of result tables, each \code{list(analysis, title, data, syntax, table_index)} -- the same shape \code{\link[=read_stat_tables]{read_stat_tables()}} returns
for \code{.jasp}/\code{.omv}/\code{.spv}, so all formats can be processed identically
downstream. \code{analysis} is the section title (e.g. \code{"MODEL RESULTS"});
\code{syntax} is the file's own recovered \verb{INPUT INSTRUCTIONS} block (the
same text on every table, since one \code{.out} file is one Mplus run).
Empty list if the file has no recoverable section.
}
\description{
Splits the file into its ALL-CAPS-headed sections (Mplus's own structural
marker -- see the file header) and extracts each section's fixed-width
result tables and label/value statistics. The \verb{INPUT INSTRUCTIONS}
section (Mplus's own verbatim analysis syntax) is skipped here -- see
\code{\link[=.mplus_export_syntax]{.mplus_export_syntax()}} for recovering it separately as a sibling \code{.inp}
file, the same way \code{\link[=.spv_export_syntax]{.spv_export_syntax()}} recovers \code{.sps} from \code{.spv}.
}
