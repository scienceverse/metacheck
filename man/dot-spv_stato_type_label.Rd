% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stato-map.R
\name{.spv_stato_type_label}
\alias{.spv_stato_type_label}
\title{Type a .spv result row by its statistic-name CELL VALUE, not a header}
\usage{
.spv_stato_type_label(label)
}
\arguments{
\item{label}{the row's own statistic-name cell value (e.g. from a
\code{"Statistics"}-labelled dimension column).}
}
\value{
the same shape \code{\link[=stato_type_column]{stato_type_column()}} returns:
\code{list(annotationValue, termSource, termAccession)}.
}
\description{
JASP/jamovi/R result tables are WIDE: one row per test, one column per
statistic (\code{t}, \code{df}, \code{p}), so \code{\link[=stato_type_column]{stato_type_column()}} types a column by its
header. \code{.spv} tables decode LONG/tidy instead (see R/spv.R): one row
per cell, with the statistic's own NAME sitting as a value inside a
dimension column (e.g. a row where the "Statistics" column reads
\code{"Sig. (2-tailed)"} and \code{value} is \code{0.037}) -- there is no header to type at
all. This types that row from its OWN category value instead, reusing the
SAME \code{.STATO_MAP}/\code{.MC_STAT_MAP} dictionaries \code{stato_type_column()} draws
on (SPSS's row labels and JASP/jamovi's column headers name the same
statistics, just positioned differently in the table), plus SPSS-specific
normalisation \code{stato_type_column()} has no reason to apply (stripping a
leading "Type III "/"Type I "/"Type II " sum-of-squares qualifier, and a
\verb{[\%1:*...]1}-style templated placeholder SPSS sometimes leaves unresolved
in a repeated-measures error-term label).
}
\keyword{internal}
