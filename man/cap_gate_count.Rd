% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report-helpers.R
\name{cap_gate_count}
\alias{cap_gate_count}
\title{Build a "cannot process this many items" count-cap message}
\usage{
cap_gate_count(
  n_needed,
  param,
  current,
  unit = "item",
  context = NULL,
  action = "process"
)
}
\arguments{
\item{n_needed}{the number of items the unit actually has}

\item{param}{the name of the parameter that caps this (e.g. \code{"file_limit"})}

\item{current}{the current value of that parameter}

\item{unit}{a noun for one item (e.g. \code{"tabular data file"})}

\item{context}{a label for the unit of work skipped (e.g. the repo URL)}

\item{action}{the verb for what was skipped (e.g. \code{"extract"}, \code{"analyse"})}
}
\value{
a single string, or \code{NULL} when \code{n_needed <= current}
}
\description{
The count analogue of \code{\link[=cap_gate_size]{cap_gate_size()}}: when a unit of work (a repository, a
codebook file, a survey) has more items than a count cap allows, the whole
unit is skipped rather than truncated. Returns \code{NULL} when within the cap.
}
\keyword{internal}
