% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/archive-dryad.R
\name{dryad_auth}
\alias{dryad_auth}
\title{Set or get Dryad OAuth2 client credentials}
\usage{
dryad_auth(
  client_id = NULL,
  client_secret = NULL,
  token_url = "https://datadryad.org/oauth/token"
)
}
\arguments{
\item{client_id}{the OAuth2 client id, or NULL to get the current client}

\item{client_secret}{the OAuth2 client secret, required when \code{client_id}
is supplied}

\item{token_url}{the token endpoint; Dryad's documented default should
not normally need overriding}
}
\value{
invisibly, the \code{httr2::oauth_client()} built from the current
credentials, or \code{NULL} when none are set
}
\description{
Dryad also issues a client id/secret pair (from the same account settings
page as the plain token in \code{\link[=dryad_pat]{dryad_pat()}}), meant for the OAuth2
client-credentials grant. Unlike a plain token, which expires after 10
hours and must be regenerated by hand, a client id/secret is exchanged for
a short-lived access token automatically -- and refreshed automatically,
for as long as the credentials themselves stay valid -- every time a
request needs one, via \code{httr2::req_oauth_client_credentials()}. This is the
right choice for a long-running corpus build; \code{\link[=dryad_pat]{dryad_pat()}} remains
available for a single quick session where generating a client id/secret
is not worth the extra step.
}
\details{
Store \code{client_id}/\code{client_secret} as the \code{DRYAD_CLIENT_ID}/
\code{DRYAD_CLIENT_SECRET} environment variables so they are read every time R
starts, the same way \code{\link[=dryad_pat]{dryad_pat()}} reads \code{DRYAD_PAT}.
}
\examples{
\dontrun{
dryad_auth("my-client-id", "my-client-secret")
}
}
