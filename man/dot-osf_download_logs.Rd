% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/archive-osf-metadata.R
\name{.osf_download_logs}
\alias{.osf_download_logs}
\title{Download an OSF project's activity log}
\usage{
.osf_download_logs(osf_id, meta_dir)
}
\arguments{
\item{osf_id}{the OSF node ID}

\item{meta_dir}{the folder to write into}
}
\value{
the log as a data frame, or NULL when it could not be read
}
\description{
Written as a CSV, because a log is tabular and is read by sorting and
filtering it. Each entry's \code{params} field is a nested structure with up to
14 members; the ones that are simple values become columns, and the few that
are themselves nested (\code{contributors}, \code{urls}, \code{params_node}) are dropped,
because a CSV cell cannot hold them. Every entry's date and action are
always kept, so the record of what happened and when is complete.
}
\keyword{internal}
