% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/code_check.R
\name{code_extract_py}
\alias{code_extract_py}
\title{Convert a Jupyter notebook to code only}
\usage{
code_extract_py(file_path = NULL, save_path = NULL, text = NULL)
}
\arguments{
\item{file_path}{path to a \code{.ipynb} file}

\item{save_path}{if NULL, returns a text vector, else a path to save to}

\item{text}{alternative to file_path, pass the notebook JSON directly}
}
\value{
a character vector of source lines (empty when the file has
no code cells or is not parseable JSON)
}
\description{
A \code{.ipynb} is a JSON document, not a text script: its source lives in the
\code{source} array of each \code{"code"} cell. This concatenates those cells in
document order so the result can be checked by the same text-based helpers
every other language goes through (\code{\link[=code_remove_comments]{code_remove_comments()}},
\code{\link[=code_library_names]{code_library_names()}}, \code{\link[=code_file_refs]{code_file_refs()}}, ...) — the notebook analogue of
\code{\link[=code_extract_r]{code_extract_r()}} purling an \code{.Rmd}/\code{.qmd}.
}
\details{
Jupyter is kernel-agnostic, so a notebook's cells may be Python \strong{or} R
(or another language); this extracts the source whatever the kernel, and
\code{\link[=code_lang]{code_lang()}} is what reports which language it is. The function is named
for the file type it reads, not for a language it assumes.

Markdown and raw cells are dropped (they are prose, not code). Cell
boundaries are preserved as blank lines so line counts stay meaningful, and
IPython magics (\verb{\%matplotlib inline}) and shell escapes (\verb{!pip install x})
are dropped: they are not statements in any kernel language and would only
ever be noise to the checks downstream.

The OUTPUTS a notebook stores alongside its code are not touched here — see
\code{\link[=read_stat_tables]{read_stat_tables()}}, which reads those as result tables the same way it
reads a \code{.jasp}/\code{.omv} archive.
}
