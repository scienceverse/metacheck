% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stat-output-TUE051134.R, R/stat-output.R
\name{stat_output_json}
\alias{stat_output_json}
\title{Serialise extracted result tables as a structured statistical-output document}
\usage{
stat_output_json(tables, paper_id = "metacheck", source_file = NA_character_)

stat_output_json(tables, paper_id = "metacheck", source_file = NA_character_)
}
\arguments{
\item{tables}{the list returned by \code{\link[=read_stat_tables]{read_stat_tables()}} or \code{\link[=read_r_output]{read_r_output()}}}

\item{paper_id}{paper id / DOI, recorded on the document}

\item{source_file}{basename of the originating \code{.jasp}/\code{.omv}, or the R
script name for run-R output, recorded as \code{source_file} and used to derive
\code{source_format} and the \code{result_id} prefix (see below)}
}
\value{
a list ready to serialise with \code{jsonlite::toJSON(auto_unbox = TRUE)}, with elements \code{schema}, \code{schema_version}, \code{paper_id},
\code{source_file}, \code{source_format}, and \code{analyses} (a list of \code{list(analysis, results)}, where each result is \code{list(result_id, row_label, values)} and
\code{values} is a named list keyed by the statistic's own short name, e.g.
\code{t}/\code{df}/\code{p}/\code{d}, each a \code{list(value, stato_label, stato_iri)} —
\code{stato_label}/\code{stato_iri} omitted when \code{\link[=stato_type_column]{stato_type_column()}} found no
STATO class for that statistic). \code{NULL} when there are no tables or no
table yields any typed value. \code{result_id} identifies the CODE ROW that
produced it — see \code{\link[=stat_results_long]{stat_results_long()}} for the id format (shared between
both serialisations of one extraction).

a list ready to serialise with \code{jsonlite::toJSON(auto_unbox = TRUE)}, with elements \code{schema}, \code{schema_version}, \code{paper_id},
\code{source_file}, \code{source_format}, and \code{analyses} (a list of \code{list(analysis, results)}, where each result is \code{list(result_id, row_label, values)} and
\code{values} is a named list keyed by the statistic's own short name, e.g.
\code{t}/\code{df}/\code{p}/\code{d}, each a \code{list(value, stato_label, stato_iri)} —
\code{stato_label}/\code{stato_iri} omitted when \code{\link[=stato_type_column]{stato_type_column()}} found no
class for that statistic). \code{NULL} when there are no tables or no
table yields any typed value. \code{result_id} identifies the CODE ROW that
produced it — see \code{\link[=stat_results_long]{stat_results_long()}} for the id format (shared between
both serialisations of one extraction).
}
\description{
Builds a complete statistical-output document from the result tables of ONE
\code{.jasp}/\code{.omv} file or executed R script: one document per source file, with
an \code{analyses} array (one element per analysis heading/table) each carrying a
\code{results} array (one element per result row) of STATO-typed statistic values
(via \code{\link[=stato_type_column]{stato_type_column()}}). This is a metacheck-native schema — a flat,
self-describing document in the character of OSD/DDI-Lifecycle/Psych-DS —
not a repurposing of an unrelated external standard's vocabulary; there is
no external schema this validates against.

Builds a complete statistical-output document from the result tables of ONE
\code{.jasp}/\code{.omv} file or executed R script: one document per source file, with
an \code{analyses} array (one element per analysis heading/table) each carrying a
\code{results} array (one element per result row) of STATO-typed statistic values
(via \code{\link[=stato_type_column]{stato_type_column()}}). This is a metacheck-native schema — a flat,
self-describing document in the character of OSD/DDI-Lifecycle/Psych-DS —
not a repurposing of an unrelated external standard's vocabulary; there is
no external schema this validates against.
}
