% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/archive-dataverse.R
\name{dataverse_pat}
\alias{dataverse_pat}
\title{Set or get a Dataverse API token}
\usage{
dataverse_pat(host, pat = NULL)
}
\arguments{
\item{host}{the Dataverse installation's hostname (e.g. "dataverse.harvard.edu")}

\item{pat}{the token to set, or NULL to get the current token}
}
\value{
the current token (character; \code{""} when none is set)
}
\description{
Dataverse installations each issue their own API token (unlike Zenodo/OSF,
which are single services) -- a token from \code{dataverse.harvard.edu} is
meaningless to \code{dataverse.nl}. Tokens are therefore stored per host.
}
\details{
Create a token by signing in to the installation, opening your account page,
and selecting "API Token".

Store it as an environment variable named \verb{DATAVERSE_PAT_<HOST>}, with the
host uppercased and every \code{.} replaced by \verb{_} (e.g.
\code{DATAVERSE_PAT_DATAVERSE_HARVARD_EDU}), so it is read every time R starts.
}
\examples{
dataverse_pat("dataverse.harvard.edu") # returns "" unless a token is set
}
