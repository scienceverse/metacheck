% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/code_check.R
\name{code_library_names}
\alias{code_library_names}
\title{Get package names loaded in code}
\usage{
code_library_names(
  code_text,
  lang = c("R", "Python", "SPSS", "SAS", "Stata", "Mplus", "MATLAB")
)
}
\arguments{
\item{code_text}{the code text for a single file}

\item{lang}{the language (R, Python, SPSS, SAS, Stata, Mplus, MATLAB)}
}
\value{
a data frame with columns \code{package}, \code{source} (how the package was
referenced: \code{library}, \code{require}, \code{requireNamespace}, \code{p_load}, \code{namespace},
\code{install}, or \code{import}), and \code{line} (the line number). Rows are unique on
\code{package} + \code{source} + \code{line}. An empty frame (same columns) when none found.
}
\description{
Extracts the names of the packages/libraries a code file loads. Where
\code{\link[=code_library_lines]{code_library_lines()}} only reports the lines on which imports occur (to check
they are grouped), this returns the actual package identifiers so they can be
catalogued and searched, or written to a \code{requirements.txt}.
}
\details{
For R this captures \code{library()} / \code{require()} / \code{requireNamespace()} (bare or
quoted argument), pacman's \code{p_load()} (which may list several packages at
once), namespace-qualified calls (\code{pkg::fun} / \code{pkg:::fun}), and
\code{install.packages()} / \code{renv::install()}. For Python it captures \verb{import a},
\verb{import a as b}, \verb{import a, b}, and \verb{from a.b import c} (recording the
top-level package \code{a}). SAS / SPSS / Stata have no package concept, so an
empty frame is returned.

The extraction is regex-based (like the rest of the module) so it degrades
gracefully on files that will not parse. It can therefore miss dynamically
constructed package names, and does not know which \emph{version} was used — only
the names appear in the source.
}
\examples{
code_text <- c(
  "library(dplyr)",
  "require('tidyr')",
  "pacman::p_load(ggplot2, readr)",
  "x <- stringr::str_trim(' a ')"
)
code_library_names(code_text, "R")
}
