% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reproducibility_check.R
\name{.repro_simple_string_vars}
\alias{.repro_simple_string_vars}
\title{Simple top-level string-literal variable assignments in a script}
\usage{
.repro_simple_string_vars(code_text)
}
\arguments{
\item{code_text}{the code text for a single file, comments already stripped}
}
\value{
a named character vector (name = literal value), possibly empty
}
\description{
A minimal constant-folder: finds assignments of the exact shape
\code{name <- "literal"} (or \code{=}/\verb{<<-}) at the START of a (comment-free) line —
the same "top level" approximation \code{\link[=repro_defined_vars]{repro_defined_vars()}} uses — and returns
the literal each name was last assigned. Only a bare quoted string is
resolved (no concatenation, no function calls); anything else leaves that
name absent from the result, so a caller asking for an unresolvable name
gets \code{NA}, never a guess.
}
\keyword{internal}
