% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/omv.R
\name{export_omv_html}
\alias{export_omv_html}
\title{Export a jamovi (.omv) file's own rendered output as standalone HTML}
\usage{
export_omv_html(path, out = NULL)
}
\arguments{
\item{path}{path to a \code{.omv} file}

\item{out}{path to write the HTML file to; defaults to \code{path} with its
extension replaced by \code{.html}, written alongside the source file}
}
\value{
the path written to, invisibly
}
\description{
A \code{.omv} archive already bundles a fully rendered \code{index.html} -- jamovi's
own output view, complete with result tables and any plots -- alongside the
data (see the file header). This extracts that \code{index.html} as-is and
inlines every plot it references (an \verb{<img src="....png">} under one of
the per-analysis \verb{resources/} folders) as a base64 \verb{data:} URI, so the
result is a single, portable, self-contained file that looks exactly like
jamovi's own output window, with no external image files to keep alongside
it.
}
