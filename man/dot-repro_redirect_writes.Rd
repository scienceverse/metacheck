% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reproducibility_check.R
\name{.repro_redirect_writes}
\alias{.repro_redirect_writes}
\title{Find a script's WRITE calls and redirect their targets into the sandbox}
\usage{
.repro_redirect_writes(code_text)
}
\arguments{
\item{code_text}{the code text for a single file (character vector)}
}
\value{
a data frame with one row per detected write call: \code{call_text} (the
full original call), \code{replacement} (the full replacement call text, or
\code{NA} when unresolved), \code{redirected_name} (the basename the write now
targets, for the read-side basename search to also check), and \code{line}
(1-based line the call starts on). Empty frame when the script has no
resolvable write calls.
}
\description{
A script writes intermediate/output files by relative path
(\code{write.csv("../Data/Cleaned/x.csv", ...)}, \code{saveRDS(m, "out/model.rds")}).
\code{\link[=repro_rewrite_paths]{repro_rewrite_paths()}} only ever rewrites READ calls (\code{code_file_refs()}'s
\code{include_writes} defaults to \code{FALSE} there) — a write's own path is left
exactly as the author wrote it, so when the materialised sandbox's directory
layout does not match the original repo's (it is Psych-DS's layout, not the
author's), the write's target directory frequently does not exist at all,
and the write fails with "cannot open the connection" — a sandboxing
artefact, not a real reproducibility finding, confirmed against a real
corpus paper whose script wrote to \code{"../Data/Cleaned/..."} (a path that only
resolves relative to the AUTHOR's own original working directory).
}
\details{
Every detected write call is redirected to \code{file.path(output_dir, basename(...))} — a single flat folder inside the sandbox that
\code{\link[=repro_materialize_layout]{repro_materialize_layout()}} always creates, so the write always succeeds
regardless of the original relative structure. Two call shapes are handled:
\itemize{
\item a LITERAL quoted target (\code{write.csv("cleaned.csv", ...)}) — redirected
directly, keeping the literal's own basename;
\item a BARE VARIABLE target (\code{write.csv(cleaned_file_name, ...)}, the variable
assigned on an earlier line, e.g. \code{cleaned_file_name <- paste0("../Data/ Cleaned/task_level_", Sys.Date(), ".csv")}) — the ENTIRE write call is
replaced with a fixed literal path, using the VARIABLE's own name (not its
unknowable runtime value, which may depend on \code{Sys.Date()} or other
non-literal pieces this scan cannot evaluate) plus the extension its
assignment's own last quoted segment declares. This does not need to
resolve the variable's exact string value — only that its assignment
LOOKS like a file path (ends \verb{.<ext>} in its last quoted piece) — so it
still succeeds even when a placeholder (\code{Sys.Date()}) makes the true
runtime value unknowable ahead of time.
}

A write whose target cannot be traced to a literal or a simple top-level
assignment (built from a loop variable, a function argument, a data value)
is left UNREWRITTEN, the same "do not guess" policy
\code{\link[=repro_rewrite_paths]{repro_rewrite_paths()}} already follows for reads.
}
\keyword{internal}
