% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/code_check.R
\name{.code_version_pin_check}
\alias{.code_version_pin_check}
\title{Detect whether a paper pinned its R/package versions}
\usage{
.code_version_pin_check(
  all_files,
  code_text_list = list(),
  max_file_size = 100,
  max_download_size = 500,
  cache = FALSE
)
}
\arguments{
\item{all_files}{the full repo file listing (\code{repo_check}'s table: needs
\code{file_name}, \code{file_url}, \code{repo_url}, \code{file_location} where available)}

\item{code_text_list}{a named list of already-read R file text (names are
file_names) — reused for the groundhog/checkpoint scan so no file is
downloaded or read twice}

\item{max_file_size}{passed to \code{\link[=download_repo_files]{download_repo_files()}} for any candidate
file not yet local}

\item{max_download_size}{passed to \code{\link[=download_repo_files]{download_repo_files()}}}

\item{cache}{passed to \code{\link[=download_repo_files]{download_repo_files()}}}
}
\value{
a list: \code{pinned} (logical, TRUE if any mechanism was found),
\code{mechanisms} (character vector, any of \code{renv.lock}, \code{sessionInfo},
\code{groundhog}, \code{checkpoint}), \code{r_versions} (character vector of R version
strings found, one per source), \code{renv_files} (character vector of
\code{renv.lock} file_names found), \code{renv_packages} (data frame \code{file_name},
\code{package}, \code{version}, \code{source} — one row per locked package, across all
\code{renv.lock} files found), \code{sessioninfo_files} (character vector of
matched file_names), \code{file_location} (named character vector, file_name
-> resolved local path, for every candidate file this call downloaded —
a caller re-checking a SUBSET of \code{all_files} per paper can splice these
back into its own copy of \code{all_files} first, so the same file is never
downloaded twice across repeat calls)
}
\description{
Checks a repository's files for the three ways authors commonly pin the
environment their analysis depended on, so a later reproduction attempt
knows which R and package versions were actually used rather than
whatever happens to be current: an \code{renv.lock} file (parsed for the R
version and every locked package + its version/source), a \code{sessionInfo()}
/ \code{sessioninfo::session_info()} text dump (matched by filename, since
authors deposit these as a dedicated file rather than burying them in
unrelated text — see \verb{@details}), or a \code{groundhog::groundhog.library()} /
\code{checkpoint::checkpoint()} call in the R code, which pin by date instead of
a lockfile. Static analysis only: this reports what was DECLARED, not
whether the declared versions still install today (that risk is what
\code{repro_install_deps()}'s CRAN Archive fallback exists for).
}
\details{
The \code{sessionInfo()} scan is filename-gated, not a blanket content scan of
every text file in the repository: it looks at files whose name suggests
they ARE such a dump (\code{sessioninfo}/\code{session_info}/\code{session-info},
case-insensitive, any extension — the two forms actually seen in a real
corpus are \code{sessionInfo.txt} and \code{session_info.txt}) plus any \verb{readme*}
file, since authors sometimes paste the dump into a README instead of a
dedicated file. A blanket scan of every \code{.txt}/\code{.md}/\code{.log} file was
considered and rejected: it would also match a paper's own methods prose
mentioning an R version in a sentence, which is not a pinned-environment
record.

\code{renv.lock} is matched by its fixed, case-sensitive filename (that is the
one name \code{renv} itself ever writes) and searched for anywhere in the
repository tree, not just the top level — a paper can deposit more than
one (e.g. one per study folder), and all are reported.

\code{groundhog}/\code{checkpoint} are detected by the PINNING CALL itself
(\code{groundhog.library(pkg, "YYYY-MM-DD")}, \code{checkpoint("YYYY-MM-DD")}), not
merely \code{library(groundhog)}/\code{library(checkpoint)}: loading the package is
not evidence it was used to pin anything, so a bare \code{library()} call does
not count.
}
\keyword{internal}
