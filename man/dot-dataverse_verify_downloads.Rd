% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/archive-dataverse.R
\name{.dataverse_verify_downloads}
\alias{.dataverse_verify_downloads}
\title{Check downloaded Dataverse files against the file system}
\usage{
.dataverse_verify_downloads(files, download_to)
}
\arguments{
\item{files}{the file table being built, with \code{path}, \code{size}, \code{checksum},
\code{checksum_type}, and \code{downloaded} columns, and optionally \code{extracted}}

\item{download_to}{the folder the dataset was saved in}
}
\value{
\code{files} with \code{downloaded} corrected against the file system, plus
\code{size_on_disk} and \code{checksum_ok} columns
}
\description{
Mirrors \code{\link[=.zenodo_verify_downloads]{.zenodo_verify_downloads()}}: confirms every file the download
planned to save is present, is the size Dataverse reported, and (when
Dataverse published an MD5 or SHA-1) matches the checksum.
}
\details{
A row whose \code{extracted} count is set is exempt from both checks, for the
same reason as the Zenodo case: Dataverse's size and checksum describe the
zip as published, and that zip was deliberately never downloaded -- only
chosen members were, each verified against the CRC32 in the archive's own
central directory as it is extracted.
}
\keyword{internal}
