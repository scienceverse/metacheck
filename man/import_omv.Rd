% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/omv.R
\name{import_omv}
\alias{import_omv}
\title{Read a jamovi (.omv) file}
\usage{
import_omv(path)
}
\arguments{
\item{path}{path to a \code{.omv} file}
}
\value{
a list with \code{data} (a data.frame; labelled columns carry haven-style
\code{label}/\code{labels} attributes), \code{columns} (a data.frame of \code{name} and \code{type}),
\code{analyses} (a character vector, one entry per analysis: its name and, when
recoverable, the reproducible R syntax), \code{format} (\code{"jamovi"}), and
\code{data_file_path} (\code{NA}; jamovi does not record the original import path).
}
\description{
Extracts the dataset, its variable metadata (measurement level, value labels)
and a summary of the analyses stored in a \code{.omv} archive. The jamovi
counterpart of \code{\link[=import_jasp]{import_jasp()}}, returning the same structure so downstream
code (codebook extraction, data checks) treats an \code{.omv} like a \code{.jasp} or
\code{.sav}.
}
