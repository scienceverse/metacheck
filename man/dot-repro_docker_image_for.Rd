% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reproducibility_check_docker.R
\name{.repro_docker_image_for}
\alias{.repro_docker_image_for}
\title{The Docker base image to use for a run}
\usage{
.repro_docker_image_for(
  r_versions = character(0),
  use_declared_version = FALSE
)
}
\arguments{
\item{r_versions}{character vector of declared R version strings (e.g.
from \code{.code_version_pin_check()$r_versions}), possibly empty}

\item{use_declared_version}{if TRUE, use \code{r_versions} (see above) instead
of the pre-built image. Default FALSE.}
}
\value{
a single image reference, e.g.
\code{"ghcr.io/scienceverse/metacheck_r:latest"}, \code{"rocker/r-ver:4.3.1"}, or
\code{"rocker/r-ver:latest"}
}
\description{
By default returns the pre-built \link{.repro_docker_default_image} regardless
of what R version the paper declared -- fast (most dependencies are
already installed), but runs against whatever R version that image was
built with, not necessarily the paper's own. When \code{use_declared_version}
is TRUE, picks the \code{rocker/r-ver} tag matching the R version
\code{\link[=.code_version_pin_check]{.code_version_pin_check()}} found declared in the paper's repository (an
\code{renv.lock}'s \code{R.Version}, or a \code{sessionInfo()} dump's first line — see
that function) instead — a bare image with nothing pre-installed, so
every dependency is installed from scratch, but it matches the paper's
own declared environment. Falls back to bare \code{rocker/r-ver:latest} (not
the pre-built image) when \code{use_declared_version = TRUE} but nothing was
declared, since there is no version to match in that case either way, and
silently reusing the pre-built image would defeat the point of asking for
version-matching in the first place.
}
\keyword{internal}
