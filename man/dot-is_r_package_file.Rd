% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_check_helpers.R
\name{.is_r_package_file}
\alias{.is_r_package_file}
\title{Identify files that belong to an R package's own source tree}
\usage{
.is_r_package_file(file_path)
}
\arguments{
\item{file_path}{a character vector of repo-relative file paths (as found
in \code{all_files$file_path})}
}
\value{
a logical vector, same length as \code{file_path}: \code{TRUE} for every
file that is part of a detected R package (the \code{DESCRIPTION}/\code{NAMESPACE}
themselves, plus their standard package subdirectories).
}
\description{
An R package repository (or a package kept in a subfolder of a larger
repository) is not itself research data or analysis code -- it is
reusable software infrastructure, and its \code{.R} files, \code{DESCRIPTION},
\code{NAMESPACE}, \verb{man/}, and \verb{tests/} are package scaffolding rather than
the study's data/code. Detected by the standard CRAN/R-package fingerprint:
a \code{DESCRIPTION} file containing a \verb{Package:} field, plus a \code{NAMESPACE}
file, both directly inside the same folder. Only that folder's own
fingerprint is checked (not, say, \code{DESCRIPTION} and \code{NAMESPACE} sitting in
two different folders that happen to share a repository).
}
\details{
A repository can hold more than one R package (e.g. a monorepo of
packages), and a package can sit in a subfolder rather than at the
repository root, so detection runs per folder rather than assuming the
whole repository is-or-isn't a package.

Detection works from file NAMES alone (no download needed): a \code{DESCRIPTION}
file and a \code{NAMESPACE} file directly inside the same folder is the
fingerprint every R package has, whether on CRAN or in a git repository
that has been through \code{devtools::document()}/\verb{R CMD build} at least once.
Only files that are actually PART of the package -- \code{DESCRIPTION}/
\code{NAMESPACE} themselves, and anything under that folder's \verb{R/}, \verb{man/},
\verb{tests/}, \verb{vignettes/}, \verb{data/}, \verb{inst/}, or \verb{src/} subdirectories -- are
reported as package files; a sibling file sitting directly in the same
folder (e.g. a study's \code{analysis.R} kept alongside a package it depends
on) is NOT swept in just because it shares that folder.
}
\keyword{internal}
