% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_check_helpers.R
\name{data_check_is_behaverse}
\alias{data_check_is_behaverse}
\alias{data_check_is_inquisit}
\alias{data_check_is_jspsych}
\alias{data_check_is_psychopy}
\title{Detect trial-level behavioural-data source formats}
\usage{
data_check_is_behaverse(df)

data_check_is_inquisit(df)

data_check_is_jspsych(df)

data_check_is_psychopy(df)
}
\arguments{
\item{df}{a data.frame (a read tabular file)}
}
\value{
\code{TRUE} when \code{df} matches the format, else \code{FALSE}.
}
\description{
Each function reports whether a data frame is an export from a particular
trial-level format, from its reserved column names. Used by codebook_check to
recognise paradata (response times, trial/stimulus channels) and normalise it
to the Behaverse \code{trial} schema rather than treating it as scale items.
}
\details{
\itemize{
\item \code{data_check_is_behaverse()} -- native Behaverse tidy long form (an
\code{instrument_id} column plus Response channels such as \code{response_numeric},
\code{response_time}, \code{trial_index}).
\item \code{data_check_is_inquisit()} -- Millisecond Inquisit \code{.iqdat} (\code{subject},
\code{blockcode}, \code{trialcode}, \code{latency}).
\item \code{data_check_is_jspsych()} -- jsPsych (\code{trial_type}, \code{rt}, and \code{trial_index}
or \code{time_elapsed}).
\item \code{data_check_is_psychopy()} -- PsychoPy Builder (\verb{<loop>.thisN}/\code{.thisRepN}
loop counters, \verb{<comp>.started}/\code{.stopped} timing, or \code{psychopyVersion} /
\code{frameRate} / \code{expName}); machinery is matched by these suffixes, not a
fixed column list, because PsychoPy column names are study-specific.
}

E-Prime is detected from file text, not a data frame (see
\code{.bh_parse_eprime()}), because its export is a header + \code{Level: 3} frames
rather than a flat table.
}
\keyword{internal}
