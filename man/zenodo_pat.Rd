% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/archive-zenodo-upload.R
\name{zenodo_pat}
\alias{zenodo_pat}
\title{Set or get the Zenodo personal access token}
\usage{
zenodo_pat(pat = NULL, sandbox = TRUE)
}
\arguments{
\item{pat}{the token to set, or NULL to get the current token}

\item{sandbox}{whether the token is for the sandbox server}
}
\value{
the current token (character; \code{""} when none is set)
}
\description{
Use \code{zenodo_pat()} to get the token used to authorise Zenodo uploads, or
\code{zenodo_pat("your-token")} to set it for the rest of the session. The sandbox
and the real Zenodo are separate services with separate accounts, so they
need separate tokens and are stored separately here.
}
\details{
To create a token, sign in and go to Applications, then "Personal access
tokens", then "New token". Give it a name and tick the \code{deposit:write} scope
(to create depositions and add files) and \code{deposit:actions} (to publish).
Copy the token immediately, as Zenodo does not show it again.
\itemize{
\item sandbox token: \url{https://sandbox.zenodo.org/account/settings/applications/tokens/new/}
\item real token: \url{https://zenodo.org/account/settings/applications/tokens/new/}
}

The sandbox needs its own account, registered at
\url{https://sandbox.zenodo.org}; a real zenodo.org login does not work there.

Store them as lines in your \code{.Renviron} file (open it with
\code{usethis::edit_r_environ()}) so they are read every time R starts:

\code{ZENODO_SANDBOX_PAT="replace-with-your-sandbox-token"}

\code{ZENODO_PAT="replace-with-your-real-token"}
}
\examples{
zenodo_pat() # returns "" unless a token is set
}
