% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reproducibility_check.R
\name{.repro_content_sniff}
\alias{.repro_content_sniff}
\title{Sniff whether a \code{.R}/\code{.Rmd} file that failed to parse is actually R}
\usage{
.repro_content_sniff(code_text)
}
\arguments{
\item{code_text}{the file's raw text (character vector, one element per
line, as read by \code{code_read()}/\code{code_extract_r()})}
}
\value{
\code{NA_character_} when nothing matches (treat as a genuine parse
failure), else one of \code{"JSON"}, \code{"HTML"}, \code{"XML"} naming what was
detected
}
\description{
A parse failure (\code{\link[=code_parse_r]{code_parse_r()}}, via \code{code_check}'s \code{parse_error}) is
usually a genuine defect in the paper's code — but occasionally the file
was never R source at all (a serialized JSON/HTML widget export saved with
a \code{.R} extension) or is a well-known non-R DSL (a JAGS/BUGS model
definition, see \code{\link[=.repro_is_jags_model]{.repro_is_jags_model()}}). Reporting either of those as
"failed to parse" reads identically to a real syntax error in real
analysis code, giving a reader no way to tell "the author has a bug" from
"this file was never R to begin with".
}
\details{
Deliberately conservative: only the UNAMBIGUOUS cases are flagged (a JSON
object/array spanning essentially the whole file, or a clear HTML/XML
doctype/tag at the very start) — checked on the RAW text, not something
\code{code_parse_r()} already tried to parse as R, so this never depends on
\emph{why} the parse failed. Anything else (including a real, if unusual, R
script) is left alone and reported as a genuine parse failure, same as
before this existed.
}
\keyword{internal}
