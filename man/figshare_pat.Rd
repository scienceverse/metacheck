% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/archive-figshare.R
\name{figshare_pat}
\alias{figshare_pat}
\title{Set or get a Figshare API token}
\usage{
figshare_pat(pat = NULL)
}
\arguments{
\item{pat}{the token to set, or NULL to get the current token}
}
\value{
the current token (character; \code{""} when none is set)
}
\description{
Figshare issues personal access tokens from account settings
(https://figshare.com/account/applications). A token is optional for
reading public articles (used here only to raise rate limits or read
private ones), unlike Dataverse where a token is per-installation.
}
\details{
Store it as the \code{FIGSHARE_PAT} environment variable so it is read every
time R starts.
}
\examples{
figshare_pat() # returns "" unless a token is set
}
