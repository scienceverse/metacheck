% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/archive-zenodo-upload.R
\name{.zenodo_flat_names}
\alias{.zenodo_flat_names}
\title{Work out the name each file will have on Zenodo}
\usage{
.zenodo_flat_names(files, folder)
}
\arguments{
\item{files}{full paths to the files being uploaded}

\item{folder}{the folder they are inside, stripped from the front}
}
\value{
a character vector of names, one per file, all distinct
}
\description{
Zenodo has no folders. Its own documentation is explicit: "Zenodo does not
support uploading and organising files into folders/directories. Instead,
you can create a ZIP archive and upload it, in which case Zenodo will
display the file structure inside the ZIP." The API bears this out -- the
bucket refuses a name containing "/" with a 404, the older form endpoint
silently rewrites \verb{code/02.R} to \code{code_02.R}, and a file object carries no
path field at all.
}
\details{
So a name has to be flat. Using the full relative path for every file makes
them needlessly long, when most files in a project have a unique name
anyway. This keeps the bare file name wherever it is unambiguous, and only
prefixes the ones that would otherwise collide -- so \code{analysis.R} stays
\code{analysis.R}, while two \code{README.md} files become \code{github__README.md} and
\code{data__README.md}.
}
\keyword{internal}
