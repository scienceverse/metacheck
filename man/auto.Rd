% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cap-prompt.R
\name{auto}
\alias{auto}
\title{Automatic (non-interactive) cap handling}
\usage{
auto(auto = NULL)
}
\arguments{
\item{auto}{\code{TRUE}/\code{FALSE} to set, or \code{NULL} (the default) to get the current
value. When unset, the value is \code{!interactive()}.}
}
\value{
the current \code{auto} setting (invisibly when setting)
}
\description{
Controls whether metacheck \strong{asks} before skipping a repository or codebook
tier that exceeds a resource cap. When \code{auto} is \code{FALSE} (and the session is
interactive), a cap that would refuse a unit of work pauses and prompts,
listing the offending files and their sizes so the choice is informed; the
user can accept the skip or raise the limit for that unit. When \code{auto} is
\code{TRUE}, no prompt is shown — the unit is skipped and the reason is reported
inline, which is the right behaviour for an unattended run.
}
\details{
The default is \code{!interactive()}, so a script run with \code{Rscript} (or any
non-interactive session) never blocks, while an interactive session asks.
}
\examples{
\dontrun{
auto(TRUE)   # never prompt; skip over-cap units and report inline
auto()       # query the current setting
}
}
