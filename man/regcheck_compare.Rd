% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db-regcheck.R
\name{regcheck_compare}
\alias{regcheck_compare}
\title{Compare a Preregistration with a Paper via RegCheck}
\usage{
regcheck_compare(
  paper_text,
  prereg_text = NULL,
  registration_id = NULL,
  client = c("ollama", "groq", "openai", "deepseek"),
  base_url = NULL,
  api_token = Sys.getenv("REGCHECK_API_TOKEN"),
  dimensions = NULL,
  reasoning_effort = "medium",
  poll_interval = 5,
  timeout = 3600
)
}
\arguments{
\item{paper_text}{the full text of the paper (single string)}

\item{prereg_text}{the text of the preregistration (single string); provide
exactly one of \code{prereg_text} or \code{registration_id}}

\item{registration_id}{a clinical trial registration ID (NCT number) to
compare against, as an alternative to \code{prereg_text}}

\item{client}{the LLM client the RegCheck server should use: "ollama"
(local), "groq", "openai", or "deepseek"}

\item{base_url}{the base URL of the RegCheck server; defaults to
\verb{http://localhost:8000} for ollama and the hosted RegCheck app otherwise
(can also be set with the \code{REGCHECK_BASE_URL} environment variable)}

\item{api_token}{the RegCheck API token}

\item{dimensions}{optional data frame with columns \code{dimension} and
\code{definition} to override the server's default comparison dimensions}

\item{reasoning_effort}{reasoning effort for OpenAI models ("low",
"medium", "high"); ignored by other clients}

\item{poll_interval}{seconds between polls of the task status}

\item{timeout}{maximum seconds to wait for the comparison to complete}
}
\value{
a data frame with one row per compared dimension, with columns
\code{dimension}, \code{deviation_judgement} ("yes", "no", or "missing"),
\code{paper_summary}, \code{prereg_summary}, \code{deviation_information},
\code{paper_quotes}, and \code{prereg_quotes}. The raw server result is attached
as the "regcheck_result" attribute.
}
\description{
Sends the full text of a paper and the text of its (pre)registration to a
\href{https://github.com/JamieCummins/regcheck}{RegCheck} server, which uses an
LLM to compare them dimension by dimension (e.g., sample size, hypotheses,
exclusion criteria) and judge for each dimension whether the paper deviates
from the registration.
}
\details{
By default this uses the locally running
\href{https://github.com/Matilda03/regcheck-fork}{RegCheck fork} with Ollama
(\code{client = "ollama"}, \verb{http://localhost:8000}), so no text leaves your
machine. Set \code{client} to \code{"groq"}, \code{"openai"} or \code{"deepseek"} to use the
hosted RegCheck app instead; note that this sends the full paper and
registration text to that server and the corresponding LLM provider.

All RegCheck servers (including a local one) require an API token. Set it
in your \code{.Renviron} (use \code{usethis::edit_r_environ()}) as:

\code{REGCHECK_API_TOKEN="your_token"}

For a local server this must match the \code{REGCHECK_API_TOKEN} you configured
when starting the server.

A comparison makes one LLM call per dimension and can take several minutes.
The function polls the server until the comparison completes.
}
\examples{
\dontrun{
paper <- demopaper()
paper_text <- paste(paper$text$text, collapse = " ")
prereg_text <- "We will collect 100 participants..."
rc <- regcheck_compare(paper_text, prereg_text)
}
}
