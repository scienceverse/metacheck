% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/repo-info-cache.R
\name{repo_info_cache}
\alias{repo_info_cache}
\title{Enable, disable, or query the repository-listing cache}
\usage{
repo_info_cache(enabled = NULL)
}
\arguments{
\item{enabled}{if logical, sets whether the cache is used; if \code{NULL}
(default) returns the current setting}
}
\value{
the current setting (logical), invisibly when setting
}
\description{
When enabled, \code{\link[=dryad_info]{dryad_info()}} and \code{\link[=zenodo_info]{zenodo_info()}} (via \code{repo_check()}'s own
\code{cache} parameter) store each repository's listing result (dataset
metadata + file list) on disk and reuse it on a later call for the same
repository, instead of re-querying the host's API. This is a SEPARATE
cache from the downloaded-file-bytes cache (see \code{\link[=repo_cache_dir]{repo_cache_dir()}}) --
listing a repository and downloading its files are different costs, and a
restart that has nothing new to download would otherwise still re-pay the
full listing cost every time.
}
\details{
A cached listing is not refreshed automatically; a published dataset's own
metadata/file list rarely changes once public, but if a paper's repository
has genuinely been updated, clear the relevant entry with
\code{\link[=repo_info_cache_clear]{repo_info_cache_clear()}}.
}
\examples{
repo_info_cache()          # is the cache on?
\dontrun{
repo_info_cache(TRUE)      # reuse listings across restarts
}
}
