% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_check_helpers.R
\name{data_strip_qualtrics_header}
\alias{data_strip_qualtrics_header}
\title{Strip Qualtrics secondary-header rows and re-type the columns}
\usage{
data_strip_qualtrics_header(df, max_strip = 2L)
}
\arguments{
\item{df}{a data.frame read from a Qualtrics export (machine names as header)}

\item{max_strip}{maximum number of leading rows to consider stripping}
}
\value{
the cleaned data.frame (unchanged if no header rows are found).
}
\description{
A Qualtrics "use choice text" export has extra header rows (human question
text, then an \code{ImportId} JSON row) directly under the machine-name header.
\code{read.delim} reads the machine names as the header but keeps those two rows as
the first data rows, which forces every column to character. This drops any
leading rows that look like Qualtrics header rows (see
\code{.qualtrics_is_header_row}) and coerces columns that are now fully numeric
back to numeric, so the rest of \code{data_check} types the file correctly.
}
\keyword{internal}
