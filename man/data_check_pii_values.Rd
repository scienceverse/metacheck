% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_check_helpers.R
\name{data_check_pii_values}
\alias{data_check_pii_values}
\title{Flag values that match a personal-information pattern}
\usage{
data_check_pii_values(x, broad_min_frac = 0.3)
}
\arguments{
\item{x}{a vector (coerced to character)}

\item{broad_min_frac}{for broad patterns, the minimum fraction of non-empty
values that must match for the column to be flagged (currently unused)}
}
\value{
list(problem, message, values) — \code{values} is the matched pattern
name(s), not the data
}
\description{
Scans a column's values for standard PII patterns (email, IP address, SSN,
credit-card-like). Reports which pattern matched and how many values, never
the matching values themselves (so the report does not leak the PII).
}
\details{
All current patterns are \emph{specific}: they flag on a single validated match,
because for disclosure one real identifier is already a problem. A raw regex
match is necessary but not sufficient — the credit-card pattern must also pass
a Luhn checksum, which keeps ordinary numbers from tripping the flag. (A
broad-pattern path with a per-column fraction threshold, \code{broad_min_frac}, is
retained for future patterns but is currently unused.)
}
\keyword{internal}
