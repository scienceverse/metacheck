% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stata.R
\name{.smcl_export_syntax}
\alias{.smcl_export_syntax}
\title{Recover a .smcl file's Stata syntax as a sibling .do file}
\usage{
.smcl_export_syntax(smcl_path, code_dir_name = "code")
}
\arguments{
\item{smcl_path}{path to the \code{.smcl} file}

\item{code_dir_name}{name of the sibling code subdirectory to write into,
relative to \code{smcl_path}'s own directory. Default \code{"code"}.}
}
\value{
the path to the written \code{.do} file, or \code{NA_character_} if the
file has no recoverable command echoes at all.
}
\description{
A \code{.smcl} file is Stata's rendered OUTPUT log, but every command it ran is
echoed verbatim (a \code{.smcl} command echo IS the exact Stata syntax, unlike
\code{.spv}'s syntax which is recovered from a SEPARATE structure element --
see \code{\link[=.spv_export_syntax]{.spv_export_syntax()}} in R/spv.R, whose role this mirrors for
\code{code_check()}). This materialises that syntax as a real \code{.do} file, in a
\code{code} subdirectory alongside the original \code{.smcl} file, so it is
discoverable the same way an author's own saved \code{.do} file would be --
and, crucially, so it then flows through \code{code_check()}'s ordinary
Stata-language analysis (comments, absolute paths, library lines)
unmodified, since \code{code_lang()} already recognises \code{.do} (see
\code{.code_expand_smcl()} in R/code_check.R, this function's only caller).
}
\keyword{internal}
