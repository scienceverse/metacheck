% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_check_helpers.R
\name{data_check_pii_freetext}
\alias{data_check_pii_freetext}
\title{Flag a free-text column that may contain incidental personal information}
\usage{
data_check_pii_freetext(
  x,
  min_median_chars = 40,
  min_unique_frac = 0.8,
  min_multiword_frac = 0.6,
  min_alpha_frac = 0.5
)
}
\arguments{
\item{x}{a character or factor vector}

\item{min_median_chars}{typical (median) length above which a column may be
free text}

\item{min_unique_frac}{minimum fraction of distinct values (prose is rarely
repeated; a coded category is)}

\item{min_multiword_frac}{minimum fraction of values that contain more than
one word (a space between word characters)}

\item{min_alpha_frac}{minimum share of alphabetic characters in the typical
value (screens out numeric/ID/hash/URL columns)}
}
\value{
list(problem, message, values)
}
\description{
Open-ended typed responses (comments, explanations, descriptions) can contain
names, places, or other identifying detail, so they warrant a "review before
sharing" prompt. The aim is to flag genuine typed prose only -- not any long,
varied string. Long values that are \emph{not} prose (numeric matrices with blank
headers, IDs, hashes, URLs, file paths, base64) are common in research data
and previously produced false positives, so a column is flagged only when its
typical value actually reads like written language:
}
\details{
\itemize{
\item long enough (\code{min_median_chars}),
\item varied enough to be responses rather than a repeated category
(\code{min_unique_frac}),
\item \strong{multi-word} -- most values contain whitespace between words, and
\item \strong{predominantly alphabetic} -- letters, not mostly digits/punctuation.
}
}
\keyword{internal}
