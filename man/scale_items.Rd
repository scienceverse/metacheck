% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scale_items.R
\docType{data}
\name{scale_items}
\alias{scale_items}
\title{Psychometric Scale Items}
\format{
A data frame with one row per item and 7 columns:
\describe{
\item{code}{OpenScales code; joins to \code{scale_meta$code}.}
\item{item_id}{Item identifier, unique within the instrument.}
\item{text}{English item wording.}
\item{dimension}{Subscale the item belongs to; empty when unassigned.}
\item{type}{Item type (\code{"likert"}, \code{"multi"}, ...).}
\item{reverse}{\code{TRUE} when the item is reverse-keyed (weight \code{-1}).}
\item{position}{1-based position within the instrument.}
}
}
\source{
OpenScales (\url{https://github.com/stmueller/OpenScales}).
Rebuild with \code{data-raw/scale_items.R}.
}
\usage{
scale_items
}
\description{
Item-level wording and reverse-key status for the instruments in
\link{scale_meta}. English wording only: items are resolved through the upstream
\code{translations[["en"]]} map, so scales published solely in another language
are absent (\code{scale_meta$languages} records what each scale offers).
}
\details{
\code{reverse} is \strong{recorded ground truth, not an inference}. OpenScales encodes
scoring as per-item weights (\code{+1} / \code{-1}) within each subscale; an item is
marked reverse here when it carries a \code{-1} weight in any subscale. Items in
a scale with no scoring block default to \code{FALSE}.
}
\seealso{
\link{scale_meta}, \link{scale_scoring}
}
\keyword{internal}
