% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report.R
\name{report_repository}
\alias{report_repository}
\title{Create a Report for a Local Repository}
\usage{
report_repository(
  path,
  output_file = NULL,
  output_format = c("html", "qmd"),
  modules = c("repo_check", "code_check", "data_check", "codebook_check"),
  args = list()
)
}
\arguments{
\item{path}{path to the repository folder to check}

\item{output_file}{the name of the output file. Defaults to the folder's own
name with \verb{_report.html} appended, written to the working directory. Give a
path here to write it somewhere else; any folders in that path must already
exist.}

\item{output_format}{the format to create the report in, \code{"html"} (the
default) or \code{"qmd"}}

\item{modules}{the modules to run. Defaults to the four repository modules,
in the order they depend on each other. Change it to run fewer.}

\item{args}{a list of extra arguments to pass to modules, named by module
(see \code{\link[=report]{report()}}). \code{local_path} and \code{local_only} are set for you.}
}
\value{
the module output, invisibly, with the report's file path in its
\code{save_path} attribute
}
\description{
Runs the repository modules on a folder of files on your own computer and
writes a single report. Use it on a repository you have downloaded (for
example with \code{\link[=osf_file_download]{osf_file_download()}}) to see what was shared and what could be
improved, before archiving the files somewhere permanent.
}
\details{
Four modules run in order, each building on the one before it:
\code{repo_check} takes an inventory of the files,
\code{code_check} reads the analysis scripts,
\code{data_check} reads the data files and runs data-quality checks,
and \code{codebook_check} checks whether the data columns are
documented. Only the first is told where the files are; the rest reuse its
results.

Nothing is downloaded and no links are followed: only the folder you name is
read. Whether a language model is used is decided by \code{\link[=llm_use]{llm_use()}}, exactly as
when running the modules individually.
}
\examples{
\dontrun{
# check a folder you have downloaded
report_repository("how_many_registered_studies_are_published")

# write the report somewhere else
report_repository("my_study", output_file = "reports/my_study.html")
}
}
