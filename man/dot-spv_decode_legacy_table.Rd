% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spv.R
\name{.spv_decode_legacy_table}
\alias{.spv_decode_legacy_table}
\title{Decode one legacy .spv table (detail-xml structure + raw case data)}
\usage{
.spv_decode_legacy_table(xml_raw, data, title = NA_character_)
}
\arguments{
\item{xml_raw}{raw vector or string: the \code{path} member's XML content (a
\verb{<visualization>} document).}

\item{data}{the decoded raw case data for this table's \code{dataPath} member,
as returned by \code{\link[=.spv_decode_legacy_data]{.spv_decode_legacy_data()}}.}

\item{title}{optional table title (from the structure reader's own
\verb{<title>} label, kept separate from this XML's own name so both formats
agree on where a title comes from).}
}
\value{
a data.frame in the same shape \code{\link[=.spv_decode_light_table]{.spv_decode_light_table()}}
returns (one row per cell, one column per dimension holding that cell's
category label, plus \code{value}), or \code{NULL} if this table uses a
construct out of this port's scope (falls through to the caller's HTML
fallback).
}
\description{
Decode one legacy .spv table (detail-xml structure + raw case data)
}
\keyword{internal}
