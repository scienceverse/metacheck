% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stata.R
\name{.smcl_render_line}
\alias{.smcl_render_line}
\title{Render one SMCL-marked-up line as plain text}
\usage{
.smcl_render_line(line)
}
\arguments{
\item{line}{one raw line of \code{.smcl} text (as read from the file, one
element of \code{readLines()}'s result)}
}
\value{
the plain-text rendering of that line
}
\description{
Interprets a single line of \code{.smcl} markup the way Stata's own Results
window / Viewer would display it: \verb{\{col N\}} jumps (padding with spaces)
to absolute column \code{N}, \verb{\{hline N\}} draws \code{N} (default 1 screen width's
worth, capped at 78 as Stata's own default window width) \code{-} characters,
\verb{\{space N\}} inserts \code{N} literal spaces, \verb{\{c NAME\}} substitutes a
box-drawing / literal-brace character (see \code{.SMCL_C_CODES}), a syntax-2/4
directive (\code{{res:text}}, \verb{\{ralign 12:text\}}) keeps just \code{text} (rendered
according to its own rule when one applies, e.g. right-alignment), and
every other directive (a style/mode marker, a link directive such as
\verb{\{help ...\}}, a comment \verb{\{* ...\}}) is a zero-width no-op that is simply
removed. Unrecognised directives are also removed rather than left in the
output verbatim, since a stray \code{{unknown}} reads far worse in extracted
text than silently dropping a directive this port has not seen.
}
\keyword{internal}
