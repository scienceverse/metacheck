% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/psychds-convert.R
\name{convert_psychds}
\alias{convert_psychds}
\title{Generate a Psych-DS-compliant copy of a repository}
\usage{
convert_psychds(
  paper,
  output_dir = NULL,
  refresh_osf = FALSE,
  local_path = NULL,
  local_only = FALSE,
  model = llm_model(),
  params = list(),
  overwrite = FALSE,
  download = "all",
  skip_types = NULL,
  peek_zips = FALSE,
  max_file_size = 100,
  max_download_size = 500,
  cache = FALSE
)
}
\arguments{
\item{paper}{a paper object (see \code{\link[=read]{read()}}), \strong{or} a captured result of
\code{report(paper, ...)} / \code{report_module_run(paper, ...)}. When a captured
result containing all of \code{data_check} / \code{codebook_check} / \code{psychds_check}
is passed, those outputs are reused (with the paper recovered from the
result) instead of re-running; otherwise the modules are run.}

\item{output_dir}{directory to write the Psych-DS dataset into; created if
needed. Defaults to \code{"psychds/<paper_id>"} under the working directory.}

\item{refresh_osf}{whether to fetch a fresh OSF file listing even if one was
already retrieved this session. The default (\code{FALSE}) reuses the cached
listing, so building the dataset right after running the checks does not
re-query (and risk being throttled by) the OSF API. Set \code{TRUE} to force a
fresh listing. Downloaded file contents are cached on disk regardless.}

\item{local_path, local_only}{passed to \code{data_check} when its output is not
already available (see \code{data_check()})}

\item{model, params}{passed to the underlying modules when \code{llm_use(TRUE)}}

\item{overwrite}{whether to overwrite an existing \code{output_dir}. When \code{FALSE}
(the default) and \code{output_dir} already exists, the function messages and
skips rather than erroring (the returned list has \code{existed = TRUE}).}

\item{download, skip_types, peek_zips, max_file_size, max_download_size, cache}{passed to \code{data_check} \strong{only when the modules must be re-run} (i.e. when
\code{paper} is a bare paper or a captured chain missing a needed module). They
are ignored when the chain already carries \code{data_check}/\code{codebook_check}/
\code{psychds_check}, whose downloaded files are reused as-is. Defaults match
\code{data_check}, except \code{download} defaults to \code{"all"} (the archive should
carry every file, not only the readable data subset); the \code{max_file_size}
and \code{max_download_size} caps still gate and warn about large files/repos.}
}
\value{
(invisibly) a list with \code{output_dir}, \code{n_files_copied},
\code{n_studies}, \code{descriptions} (paths of written dataset_description.json
files), \code{collection} (path of the root \code{ro-crate-metadata.json} for a
multi-study collection, else empty), \code{fulltext} (path of the paper's full-text file
under \verb{documentation/}, if written), and \code{logs} (paths written into
\verb{logs/}: the file manifest, and the check results / module tables when those
modules ran). When an existing \code{output_dir} was skipped, the list
additionally contains \code{existed = TRUE} and the counts are zero.
}
\description{
Writes a \href{https://psych-ds.github.io/}{Psych-DS} dataset directory for a
paper's data repository: files are copied to their standard locations, a
\code{dataset_description.json} is generated (with \code{variableMeasured} built from
the extracted columns and codebook labels), and \code{README} / \code{CHANGES} stubs
are added when missing. This is the generator counterpart of the
\code{psychds_check} module — run the check first to preview the gap.
}
\details{
Multi-study repositories (when \code{data_check} assigned study groups under
\code{llm_use(TRUE)}) are written as \verb{study-<group>/} subdirectories, each a
self-contained, independently-valid Psych-DS dataset. Files that belong to no
single study (a whole-repo README/codebook, shared materials) sit at the
collection root beside the study folders (following BIDS, which places shared
content at the root rather than in a pseudo-subject). The root is then a
\emph{collection} of datasets, not itself a Psych-DS dataset, so it carries a
machine-readable \code{ro-crate-metadata.json} (RO-Crate 1.3 JSON-LD, with a
custom context for DDI-Lifecycle-inspired study-design/variable terms)
instead of a root \code{dataset_description.json} — validate each \verb{study-*/} folder.
Original files whose contents cannot be read (no local copy) are skipped with
a note.

If the reused/re-run chain includes \code{reproducibility_check}'s result AND that
call was made with \code{keep_sandbox = TRUE}, its \verb{statistical_output/} folder
(extracted, STATO-typed statistics — see \code{\link[=reproducibility_check]{reproducibility_check()}}) is copied
into \verb{output_dir/statistical_output/}, sibling to \verb{output_dir/data/}. This
converter does \strong{not} run \code{reproducibility_check} itself and does not force
\code{keep_sandbox = TRUE} on your behalf (doing so would mean every conversion
also runs — or even executes — the paper's code, whether or not you asked
for statistical output); when the chain does not carry a kept sandbox, this
folder is silently omitted from the archive.
}
\examples{
\dontrun{
# Capture the checks, then build the folder — the modules are reused, not
# re-run, and the OSF listing is not fetched again:
res <- report(paper, c("data_check", "codebook_check", "psychds_check"))
convert_psychds(res)

# Or call directly on the paper; the modules are run as needed:
convert_psychds(paper)
}
}
