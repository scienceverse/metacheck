% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stat-output.R
\name{stat_output_validate}
\alias{stat_output_validate}
\title{Validate a statistical-output document's native shape}
\usage{
stat_output_validate(doc)
}
\arguments{
\item{doc}{a statistical-output document as an R list (as from
\code{\link[=stat_output_json]{stat_output_json()}}), or a length-1 character path / JSON string to parse
first.}
}
\value{
a list with \code{valid} (\code{TRUE} when no issues), \code{issues} (a character
vector of problem descriptions), and \code{summary} (\code{n_errors}, \code{n_analyses},
\code{n_results}).
}
\description{
Checks that a \code{\link[=stat_output_json]{stat_output_json()}} document has the required top-level
fields, that \code{analyses} is a list of \code{list(analysis, results)}, and that
every result carries a \code{result_id} and a non-empty \code{values} object whose
entries each have a \code{value}. This is a native structural check, not an
executed external JSON Schema — there is no external standard this document
conforms to, by design (see the file header comment). Mirrors
\code{behaverse_validate()}'s and \code{psychds-validate.R}'s hand-rolled approach.
}
