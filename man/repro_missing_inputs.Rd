% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reproducibility_check.R
\name{repro_missing_inputs}
\alias{repro_missing_inputs}
\title{Diagnose why a referenced input file is unavailable}
\usage{
repro_missing_inputs(refs, plan, structure_df, skipped = NULL)
}
\arguments{
\item{refs}{a character vector of referenced file basenames (unresolved reads)}

\item{plan}{the \code{psychds_check} table (file_name + target_path)}

\item{structure_df}{the \code{data_check} structure table (file_name +
file_location + file_size)}

\item{skipped}{optional data frame of size-skipped files (the
\code{oversize_skipped} / \code{gated} attributes of \code{\link[=download_repo_files]{download_repo_files()}}, with a
\code{file_name} column)}
}
\value{
a data frame with \code{basename}, \code{status} (one of \code{in_repo_not_downloaded},
\code{withheld_size}, \code{absent}), and \code{detail} (a human-readable reason).
}
\description{
When a script reads a file that is not present, the reason matters: a file
deliberately withheld because it was too large to download is a different
finding from a file simply absent from the repository. This cross-references
the referenced basenames against the file plan and the download-skip records
\code{download_repo_files()} attaches, and classifies each unresolved reference.
}
