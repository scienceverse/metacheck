% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/match-table.R
\name{.table_tests}
\alias{.table_tests}
\title{Build reported-test elements from a paper's table contents}
\usage{
.table_tests(paper)
}
\arguments{
\item{paper}{a paper object}
}
\value{
a list of test elements, \code{match_reported_output()}'s internal shape
}
\description{
The table-side counterpart to \code{\link[=extract_tests]{extract_tests()}}/\code{.recompose_eq()}: for every
table with parsed \code{contents} (see \code{.tei_table_contents()} in
R/import-grobid.R), types each cell via header, caption, and value-shape (see
this file's header comment for the full 5-tier fallback), then bundles a
row's typed cells into one test — or, for a matrix-shaped table (a
correlation matrix), matches each matrix cell as its own independent
single-value test. Used by \code{\link[=match_reported_output]{match_reported_output()}} when
\code{include_tables = TRUE}; not exported because its output shape (a plain list
of \code{list(text_id, grp_id, components)}) is match-reported.R's own internal
convention, not a user-facing table.
}
\keyword{internal}
