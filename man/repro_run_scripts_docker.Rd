% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reproducibility_check_docker.R
\name{repro_run_scripts_docker}
\alias{repro_run_scripts_docker}
\title{Run the paper's scripts, in order, each in an isolated Docker container}
\usage{
repro_run_scripts_docker(
  run_tbl,
  order,
  sandbox_root,
  lib_dir = NULL,
  image = "rocker/r-ver:latest",
  timeout = 600,
  skip = character(0),
  parses = NULL,
  failed_deps = character(0)
)
}
\arguments{
\item{run_tbl}{\code{repro_write_scripts()} output (\code{file_name}, \code{script_path},
\code{run_dir}) — \code{run_dir} must be the SAME directory as (or a subdirectory
of) \code{sandbox_root}, so it can be bind-mounted as a whole}

\item{order}{a vector of \code{file_name}s in the order to run}

\item{sandbox_root}{the materialised layout root on the HOST (from
\code{\link[=repro_materialize_layout]{repro_materialize_layout()}}) — mounted read-write at \verb{/sandbox}}

\item{lib_dir}{throwaway library ON THE HOST (or NULL) — mounted
read-only at \verb{/rlib} when supplied}

\item{image}{the Docker image to run scripts in (see \code{\link[=.repro_docker_image_for]{.repro_docker_image_for()}})}

\item{timeout}{per-script timeout in seconds}

\item{skip}{character vector of \code{file_name}s to record as
\code{skipped_missing_inputs} instead of running}

\item{parses}{named logical (by file_name); a file that will not parse is
recorded \code{not_parsed} and not run}

\item{failed_deps}{character vector of package names
\code{\link[=repro_install_deps_docker]{repro_install_deps_docker()}} could not install}
}
\value{
a data frame — identical columns to \code{\link[=repro_run_scripts]{repro_run_scripts()}}'s
return: \code{file_name}, \code{outcome}, \code{error}, \code{error_type}, \code{undefined_var},
\code{stdout}, \code{stderr}, \code{elapsed}, \code{script_lines}, \code{captures}
}
\description{
The Docker-backed twin of \code{\link[=repro_run_scripts]{repro_run_scripts()}}: same return contract, same
per-script semantics (skip on missing inputs / parse failure, timeout,
undefined-variable / dependency-unavailable classification, captured
result objects), but each script runs via \verb{docker run --network none --read-only --user <non-root>} instead of a \code{callr} subprocess — so the
code cannot reach the network or touch anything outside the mounted
sandbox and library, even deliberately. See this file's header comment for
the two-phase (install-then-run) design and why the library is mounted at
\verb{/rlib}, not \verb{/lib}.
}
