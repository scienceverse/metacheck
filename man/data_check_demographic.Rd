% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_check_helpers.R
\name{data_check_demographic}
\alias{data_check_demographic}
\title{Detect whether a column holds participant age, gender/sex, or race/ethnicity}
\usage{
data_check_demographic(col_name, x)
}
\arguments{
\item{col_name}{the column's name}

\item{x}{the column's values}
}
\value{
\code{"age"}, \code{"gender"}, or \code{"race"} when the column matches one of
them, else \code{NA_character_}.
}
\description{
A content-based classifier for the three demographic variables collected by
almost every human-subjects study. A column is tagged only when its NAME
looks like the demographic AND its VALUES are consistent with it (see
\link{.demographic_values_ok}), which keeps false positives low: a \code{condition}
column coded 1/2 is not flagged as gender, and an \code{age} column of free text
is not treated as usable age data.
}
\details{
Complements \code{\link[=data_col_type]{data_col_type()}} (which gives a structural type such as
continuous/categorical): this adds a \emph{semantic} label used by \code{data_check}
(reported in the column table) and \code{data_validate} (which reports the
demographics a file contains). Detection is name-driven, so a demographic
under a cryptic name (e.g. \code{q3}) is intentionally not caught here — that is
the LLM classifier's job.
}
\examples{
data_check_demographic("age", c(23, 45, 31, 29))
data_check_demographic("gender", c("Male", "Female", "Female", "Male"))
data_check_demographic("condition", c(1, 2, 1, 2))   # NA (name does not match)
}
\keyword{internal}
