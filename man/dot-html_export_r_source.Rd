% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/html-output.R
\name{.html_export_r_source}
\alias{.html_export_r_source}
\title{Recover R source from a knitted R Markdown / Quarto HTML file}
\usage{
.html_export_r_source(html_path, code_dir_name = "code")
}
\arguments{
\item{html_path}{path to a local \code{.html} file already confirmed (by
\code{\link[=.html_sniff_kind]{.html_sniff_kind()}}) to be \code{"rmd"}}

\item{code_dir_name}{the sibling folder to write into, same convention as
\code{\link[=.spv_export_syntax]{.spv_export_syntax()}} / \code{\link[=.smcl_export_syntax]{.smcl_export_syntax()}} (default \code{"code"})}
}
\value{
the path to the written \code{.R} file, or \code{NA_character_} if the
document could not be read or contained no recoverable R chunk
}
\description{
Extracts every input-code chunk pandoc/knitr rendered into the document
(\verb{<pre class="r"><code>...</code></pre>} or \verb{<div class="sourceCode r">}),
in document order, and writes them — separated by a blank line, matching
how \code{\link[=code_read]{code_read()}} already treats whitespace between statements — as a
sibling \code{.R} file. This is the HTML analogue of \code{\link[=.smcl_export_syntax]{.smcl_export_syntax()}} /
\code{\link[=.spv_export_syntax]{.spv_export_syntax()}}: the ORIGINAL analysis code sits verbatim inside the
rendered document (HTML-entity-escaped only, never altered), so it is
exactly recoverable, not reconstructed or guessed.
}
\details{
Printed console OUTPUT (\verb{## }-prefixed lines pandoc/knitr interleave after
each chunk) is deliberately NOT included: this produces a re-runnable \code{.R}
script for \code{code_check}/\code{reproducibility_check} (the modules in
\verb{inst/modules/}), and a \verb{##}-prefixed line is a comment in R syntax anyway,
so leaving it out changes nothing a parse would see — it is dropped here
only to keep the recovered file to actual code.
}
\keyword{internal}
