% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reproducibility_check.R
\name{repro_defined_vars}
\alias{repro_defined_vars}
\title{Find the variables each script defines at top level}
\usage{
repro_defined_vars(code_text_list)
}
\arguments{
\item{code_text_list}{a named list of code-text character vectors, one per
file (names are the file_names)}
}
\value{
a data frame with \code{file_name} and a list-column \code{defines} (character
vector of variable names each file assigns at top level).
}
\description{
When a script errors with \verb{object 'X' not found}, the fix is usually to run
the script that \emph{defines} \code{X} first. This scans each file for \strong{top-level}
variable assignments — \code{x <- ...}, \code{x = ...}, \code{x <<- ...}, and
\code{assign("x", ...)} — so an undefined-variable error can be matched to the file
that would supply it (see the module's corrective re-run). Only assignments at
the start of a (comment-free) line are taken, which approximates "top level":
an assignment indented inside a \code{function}/\code{for} body is not a global the next
sourced script would see, and taking it would create false ordering edges.
}
