% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reproducibility_check.R
\name{repro_run_scripts}
\alias{repro_run_scripts}
\title{Run the paper's scripts, in order, in isolated subprocesses}
\usage{
repro_run_scripts(
  run_tbl,
  order,
  lib_dir = NULL,
  timeout = 600,
  skip = character(0),
  parses = NULL
)
}
\arguments{
\item{run_tbl}{\code{repro_write_scripts()} output (\code{file_name}, \code{script_path},
\code{run_dir})}

\item{order}{a vector of \code{file_name}s in the order to run (unplaceable files
are appended at the end in input order)}

\item{lib_dir}{throwaway library to expose to each subprocess (or NULL)}

\item{timeout}{per-script timeout in seconds}

\item{skip}{character vector of \code{file_name}s to record as
\code{skipped_missing_inputs} instead of running}

\item{parses}{named logical (by file_name); a file that will not parse is
recorded \code{not_parsed} and not run}
}
\value{
a data frame with \code{file_name}, \code{outcome} (one of \code{ran_ok},
\code{errored}, \code{timed_out}, \code{not_parsed}, \code{skipped_missing_inputs}), \code{error}
(message or ""), \code{error_type} (\code{undefined_variable}, \code{timeout}, \code{runtime},
or NA), \code{undefined_var} (the missing variable name when
\code{error_type == "undefined_variable"}, else NA), \code{stdout}, \code{stderr},
\code{elapsed} (seconds), and \code{script_lines} (list-column: the EXECUTED script's
own text, one element per source line — the exact text \code{stdout}'s echoed
statements were run from, so \code{\link[=read_r_output]{read_r_output()}} can match them back to a
line number; empty character(0) for a script that did not run).
}
\description{
Runs each script with \code{\link[callr:r]{callr::r()}} from \code{run_dir}, with \code{lib_dir} prepended to
the subprocess \code{.libPaths()} so the throwaway library is seen. Each run is
bounded by \code{timeout} seconds; its exit, error, stdout and stderr are captured.
A script whose inputs are known-missing (in \code{skip}) is not run — it is
recorded \code{skipped_missing_inputs}, since it cannot succeed and would only
produce a confusing error. callr isolates a crash to the subprocess; it does
not sandbox the filesystem or network, so this only runs on explicit opt-in.
}
