% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/code_check.R
\name{code_extract_qmd_py}
\alias{code_extract_qmd_py}
\title{Extract Python chunks from a Quarto document}
\usage{
code_extract_qmd_py(file_path = NULL, save_path = NULL, text = NULL)
}
\arguments{
\item{file_path}{a path to a \code{.qmd} file}

\item{save_path}{if NULL, returns a text vector, else a path to save to}

\item{text}{alternative to file_path, pass text directly}
}
\value{
a character vector of Python source lines (empty when the
document has no Python chunks)
}
\description{
A \code{.qmd} whose declared/inferred engine is Python (see \code{\link[=code_lang]{code_lang()}}'s
\code{.qmd_lang()}) cannot go through \code{\link[=code_extract_r]{code_extract_r()}}: \code{knitr::purl()} only
recovers \verb{```\{r\}} chunks, so a Python-engine document would purl to
nothing. This is the \code{.qmd} analogue of \code{\link[=code_extract_r]{code_extract_r()}} for that case —
concatenating the body of every \verb{```\{python\}} fence in document
order, the same "recover a checkable code file" idea \code{\link[=code_extract_py]{code_extract_py()}}
applies to a \code{.ipynb}'s code cells (chunk boundaries are preserved as a
blank line so line counts stay meaningful).
}
\details{
Unlike \code{\link[=code_extract_r]{code_extract_r()}}, this is a plain text/regex scan, not a real
Pandoc/Quarto parse: a fence marker appearing inside a string literal or a
displayed (non-executable) code block written with four backticks around a
literal \verb{```\{python\}} would be misread as a real chunk. This
mirrors the fence-based approach already used elsewhere in the module
(e.g. \code{code_parse_r()}'s own \verb{^---\\\\s*$} front-matter check) rather than
introducing a new parsing dependency for a single edge case.
}
