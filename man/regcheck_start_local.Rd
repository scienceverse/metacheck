% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regcheck-local.R
\name{regcheck_start_local}
\alias{regcheck_start_local}
\title{Start the local RegCheck server}
\usage{
regcheck_start_local(method = c("docker", "python"), model = NULL, port = 8000)
}
\arguments{
\item{method}{\code{"docker"} (default) or \code{"python"}}

\item{model}{Ollama language model to use. Defaults to \code{NULL}, which
auto-selects the largest language model currently pulled in Ollama. Any
model you have pulled with \verb{ollama pull} can be used here -- larger
models generally produce better judgements.}

\item{port}{port to run the server on (default 8000)}
}
\value{
invisibly, the process object
}
\description{
Starts the bundled RegCheck server as a background process, either via
Docker (recommended) or a manual Python virtual environment. The server
runs at \verb{http://localhost:8000} and is used automatically by
\code{module_run(paper, "reg_check")} (the default \code{client = "ollama"}).
}
\details{
\strong{Docker path} (\code{method = "docker"}, recommended):
Requires \href{https://www.docker.com/get-started/}{Docker} to be installed.
The first run builds the image (~5 minutes); subsequent starts are instant.
No Python installation needed.

\strong{Python path} (\code{method = "python"}):
Requires Python 3.12+ and Ollama. Run \code{\link[=regcheck_setup_local]{regcheck_setup_local()}} once first.

In both cases, Ollama must be running with the required models pulled.
The embedding model is fixed; the language model can be any Ollama model
you have pulled -- use the largest one that fits your hardware:

\if{html}{\out{<div class="sourceCode">}}\preformatted{ollama pull nomic-embed-text-v2-moe
ollama pull llama3.2          # or llama3.1, mistral, etc.
}\if{html}{\out{</div>}}

The API token is set automatically to \code{"metacheck-local"} -- no manual
token configuration needed.
}
