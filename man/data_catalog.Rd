% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-catalog.R
\name{data_catalog}
\alias{data_catalog}
\title{Build an archive-level catalog for a data release}
\usage{
data_catalog(
  archive_dir,
  papers = NULL,
  name = "metacheck data release",
  description = "A collection of Psych-DS datasets built by metacheck.",
  manifest_dir = NULL
)
}
\arguments{
\item{archive_dir}{the directory holding the per-paper archive folders (each a
\verb{<paper_id>/} with a Psych-DS \code{dataset_description.json})}

\item{papers}{optional paperlist (see \code{\link[=papers_load]{papers_load()}}) used to enrich each
entry with title / authors / DOI when a folder's descriptor lacks them}

\item{name}{a name for the catalog (e.g. \code{"Open Mind data release"})}

\item{description}{a one-line description of the catalog}

\item{manifest_dir}{optional directory of per-paper manifests (see
\code{data_check(manifest=)}); when given, each entry records its file count and
total size from the manifest}
}
\value{
(invisibly) the path to the written \code{catalog.json}
}
\description{
Scans a built Psych-DS archive directory (as produced by \code{\link[=convert_psychds]{convert_psychds()}}
per paper) and writes a top-level \code{catalog.json}: a schema.org
\href{https://schema.org/DataCatalog}{\code{DataCatalog}} listing every paper's dataset,
with its title, authors, DOI, and a pointer to the paper's own
\code{dataset_description.json}. This is the object that makes the release a single
discoverable archive rather than a set of unconnected folders; it is harvested
by Google Dataset Search and is the schema.org equivalent of a DDI study
group (each entry a study, the per-paper descriptor its DDI-Codebook).
}
\details{
A companion \code{catalog.csv} (one row per dataset) is written alongside for quick
inspection.
}
\examples{
\dontrun{
papers <- papers_load("openmind")
data_catalog("output/openmind", papers = papers,
             name = "Open Mind data release",
             manifest_dir = "output/openmind/_manifests")
}
}
