% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report-helpers.R
\name{cap_gate_size}
\alias{cap_gate_size}
\title{Build a "cannot download this repository" size-cap message}
\usage{
cap_gate_size(
  repo,
  n_files,
  total_mb,
  oversized,
  max_file_size,
  max_download_size
)
}
\arguments{
\item{repo}{the repository URL (for the message)}

\item{n_files}{number of files that would be downloaded}

\item{total_mb}{total size of the download in MB (known sizes)}

\item{oversized}{a data.frame with columns \code{name} and \code{size_mb} for files
individually larger than \code{max_file_size} (may be empty)}

\item{max_file_size}{the per-file cap in MB}

\item{max_download_size}{the per-repository total cap in MB}
}
\value{
a single string, or \code{NULL} when the repository fits within the caps
}
\description{
Caps are an upfront, all-or-nothing gate: when a repository cannot be
downloaded \emph{in full} within the size limits, the whole repository is refused
rather than partially downloaded. This builds the warning that names both
size parameters, their current values, the offending files, and the values
needed to proceed. Returns \code{NULL} when the repository fits (so the caller can
treat \code{NULL} as "go ahead").
}
\keyword{internal}
