% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/archive-zenodo.R
\name{.zenodo_verify_downloads}
\alias{.zenodo_verify_downloads}
\title{Check downloaded Zenodo files against the file system}
\usage{
.zenodo_verify_downloads(files, download_to)
}
\arguments{
\item{files}{the file table being built, with \code{path}, \code{size}, \code{checksum},
and \code{downloaded} columns, and optionally \code{extracted}}

\item{download_to}{the folder the record was saved in}
}
\value{
\code{files} with \code{downloaded} corrected against the file system, plus
\code{size_on_disk} and \code{checksum_ok} columns
}
\description{
Confirms that every file the download planned to save is present, is the
size Zenodo reported, and matches the checksum Zenodo published for it.
}
\details{
Up to this point \code{downloaded} records only that the transfer step ran. A
request that failed, a copy that did not happen, or a truncated write all
leave a row marked TRUE with nothing usable on disk. That matters most when
archiving a whole record unattended, where nobody is watching each file.

Zenodo publishes an MD5 checksum per file, which catches corruption that a
size comparison cannot -- a file can be the right length and still be wrong.
Hashing uses \code{tools::md5sum()} from base R, as \code{import-grobid.R} and
\code{llm-cache.R} already do, so this adds no dependency. Only files Zenodo gave
an \verb{md5:} value for are hashed; for anything else the size check stands on
its own.

A row whose \code{extracted} count is set is exempt from both checks. Zenodo's
size and MD5 describe the zip as published, and that zip was deliberately
never downloaded -- only chosen members were. Measured against those values
every such row would look truncated. The members carry their own integrity
check instead: each is verified against the CRC32 stored in the archive's
central directory as it is extracted, in \code{.zip_member_fetch()}.
}
\keyword{internal}
