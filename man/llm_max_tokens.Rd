% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/llm.R
\name{llm_max_tokens}
\alias{llm_max_tokens}
\title{Set the default max_tokens for LLM calls}
\usage{
llm_max_tokens(n = NULL)
}
\arguments{
\item{n}{the default max_tokens value, or \code{NULL} to read the current value
without changing it}
}
\value{
the current default max_tokens value (invisibly when setting)
}
\description{
\code{llm()} needs SOME max_tokens value on every call (a provider truncates a
response with no clear error otherwise — a codebook/data-check pass over
many columns at once is the case most likely to need more than the
built-in default). Rather than requiring every module call to repeat
\code{params = list(max_tokens = ...)} individually, set it once per session
here; \code{llm()} falls back to this whenever a caller's own \code{params} does not
already specify \code{max_tokens} (an explicit \code{params$max_tokens} on a single
call still wins, same precedence as every other \code{params} entry).
}
\examples{
\dontrun{
llm_max_tokens(8192)
}
}
