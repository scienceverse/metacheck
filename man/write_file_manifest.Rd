% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/archive-report.R
\name{write_file_manifest}
\alias{write_file_manifest}
\title{Build a corpus-wide file inventory from per-paper manifests}
\usage{
write_file_manifest(manifest_dir, out = NULL)
}
\arguments{
\item{manifest_dir}{directory holding the per-paper \verb{*.manifest.json} files.}

\item{out}{optional path to write the inventory CSV to. \code{NULL} (default)
writes \verb{_all_files.csv} inside \code{manifest_dir}; \code{NA} writes nothing (the data
frame is only returned).}
}
\value{
the inventory as a data.frame, invisibly. Columns: \code{paper_id},
\code{repo_url}, \code{file_name}, \code{file_path}, \code{file_url}, \code{file_size},
\code{file_size_mb}, \code{data_type}, \code{downloaded}, \code{status}, \code{skip_intentional},
\code{skip_reason}.
}
\description{
Reads every \verb{*.manifest.json} in \code{manifest_dir} (written by \code{data_check()}
when its \code{manifest} argument is set) and flattens them into a single table:
one row per repository file across all papers, with its download status and
skip reason. This is the whole-archive view of what exists versus what was
fetched.
}
\details{
It is the reusable form of the inventory step a batch script runs at the end;
because it reads only the on-disk manifests, it can be run at any time — after
an interrupted run, or to refresh the inventory once more files download on a
re-run.
}
\examples{
\dontrun{
write_file_manifest("output/openmind/_manifests")
}
}
\seealso{
\code{\link[=collect_check_results]{collect_check_results()}}, \code{data_check()}
}
