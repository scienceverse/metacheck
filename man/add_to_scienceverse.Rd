% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scienceverse.R
\name{add_to_scienceverse}
\alias{add_to_scienceverse}
\title{Add a metacheck collection to a scienceverse SQLite archive}
\usage{
add_to_scienceverse(
  collection_root,
  db_path = .sv_default_db(),
  rebuild_fts = TRUE,
  quiet = FALSE
)
}
\arguments{
\item{collection_root}{path to a metacheck output root — a folder containing
\code{collection.json} (a multi-study collection) or \code{dataset_description.json}
(a single-study Psych-DS dataset), as written by \code{\link[=convert_psychds]{convert_psychds()}}. A
character vector adds several in one call.}

\item{db_path}{path to the SQLite file to create or append to. Created (with
parent directories) if it does not exist. Defaults to
\code{getOption("metacheck.scienceverse.db")} when set, else a canonical
\code{scienceverse.sqlite} in the package's rappdirs data directory
(\code{rappdirs::user_data_dir("metacheck", "scienceverse")}), so repeated calls
accumulate into one shared archive.}

\item{rebuild_fts}{whether to rebuild the FTS5 indexes after writing. \code{TRUE}
(default) keeps search current; set \code{FALSE} when adding many roots in a
loop and call the function once more (or \code{add_to_scienceverse()} on the last
root) to rebuild at the end.}

\item{quiet}{suppress progress messages.}
}
\value{
invisibly, \code{db_path}.
}
\description{
Shred a metacheck Psych-DS collection (as written by \code{\link[=convert_psychds]{convert_psychds()}})
into a searchable SQLite database. Run it right after \code{convert_psychds()} on
the collection root it produced, or over any existing collection root to
backfill the archive. Adding the same paper again replaces its rows, so the
operation is idempotent.
}
\details{
The archive stores structural tables (\code{papers}, \code{studies}, \code{variables},
\code{scales}, \code{files}, \code{checks}), five findings tables split by module domain
(\code{stat_findings}, \code{code_findings}, \code{data_findings}, \code{excel_findings},
\code{other_findings}), the lossless checks result as a BLOB in \code{papers.checks_rds},
the manuscript full text in \code{papers.fulltext}, and FTS5 full-text indexes
(\code{papers_fts}, \code{findings_fts}, \code{variables_fts}, \code{scales_fts}).

Requires the suggested packages \pkg{DBI} and \pkg{RSQLite}.
}
\examples{
\dontrun{
res <- convert_psychds(paper, output_dir = "psychds/mypaper")
add_to_scienceverse(res$output_dir, "scienceverse.sqlite")

# backfill an existing corpus
roots <- list.dirs("collabra", recursive = FALSE)
add_to_scienceverse(roots, "scienceverse.sqlite")

# query it
con <- DBI::dbConnect(RSQLite::SQLite(), "scienceverse.sqlite")
DBI::dbGetQuery(con, "
  SELECT DISTINCT p.doi, p.title
  FROM papers p
  JOIN papers_fts f ON f.doi = p.doi
  JOIN stat_findings s ON s.doi = p.doi
  WHERE papers_fts MATCH 'stress' AND s.f_reported > 5")
DBI::dbDisconnect(con)
}
}
