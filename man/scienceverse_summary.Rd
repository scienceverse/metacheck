% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scienceverse-TUE051134.R, R/scienceverse.R
\name{scienceverse_summary}
\alias{scienceverse_summary}
\title{Get a paper's summary table}
\usage{
scienceverse_summary(con, doi)

scienceverse_summary(con, doi)
}
\arguments{
\item{con}{a scienceverse connection.}

\item{doi}{the paper identifier.}
}
\value{
a data frame with columns \code{metric} and \code{value}.

a data frame with columns \code{metric} and \code{value}.
}
\description{
Return the per-paper summary_table (the wide row of per-module counts) as a
tidy two-column data frame (\code{metric}, \code{value}), for display on the paper
detail. Empty when the paper has no stored summary.

Return the per-paper summary_table (the wide row of per-module counts) as a
tidy two-column data frame (\code{metric}, \code{value}), for display on the paper
detail. Empty when the paper has no stored summary.
}
