% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mplus.R
\name{.mplus_export_syntax}
\alias{.mplus_export_syntax}
\title{Recover a .out file's Mplus syntax as a sibling .inp file}
\usage{
.mplus_export_syntax(out_path, code_dir_name = "code")
}
\arguments{
\item{out_path}{path to the \code{.out} file}

\item{code_dir_name}{name of the sibling code subdirectory to write into,
relative to \code{out_path}'s own directory. Default \code{"code"}.}
}
\value{
the path to the written \code{.inp} file, or \code{NA_character_} if the
file has no recoverable \verb{INPUT INSTRUCTIONS} section.
}
\description{
A \code{.out} file's own \verb{INPUT INSTRUCTIONS} section IS the exact Mplus
syntax that produced it -- unlike \code{.spv} (syntax recovered from a
SEPARATE structure element), and like \code{.smcl} (a command echo IS the
syntax). This materialises that block as a real \code{.inp} file (Mplus's own
input-syntax extension), in a \code{code} subdirectory alongside the original
\code{.out} file, so it is discoverable the same way an author's own saved
\code{.inp} file would be -- see \code{.code_expand_mplus()} in R/code_check.R,
this function's only caller.
}
\keyword{internal}
