% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reproducibility_check.R
\name{repro_rewrite_paths}
\alias{repro_rewrite_paths}
\title{Rewrite a code file's data-file paths to the Psych-DS layout}
\usage{
repro_rewrite_paths(
  code_text,
  file_name,
  plan,
  lang = "R",
  structure_df = NULL
)
}
\arguments{
\item{code_text}{the code text for a single file (character vector)}

\item{file_name}{the script's own name/path (used to derive its study group)}

\item{plan}{the \code{psychds_check} table: one row per file with \code{file_name},
\code{target_path}, \code{current_path}, and (for converted tabular files)
\code{original_target}}

\item{lang}{the language (only R is rewritten here)}

\item{structure_df}{optional \code{data_check} structure table (\code{file_name},
\code{file_location}) — when supplied, enables the content-hash mirror
collapsing described above. \code{NULL} (default) skips it.}
}
\value{
a data frame with one row per referenced path, columns \code{ref} (the
path as written, or the full call text when \code{is_call}), \code{basename},
\code{matched} (logical, a plan file was found), \code{target} (its Psych-DS path,
or NA), \code{ambiguous} (logical, several plan files matched and the study
could not disambiguate), \code{n_candidates}, and \code{is_call} (logical, TRUE when
\code{ref} is a whole \code{sprintf()}/\code{paste()}-family call rather than a literal
path — see above). Empty frame when the script references no files.
}
\description{
A script reads and writes data by relative path (\code{read_csv("raw/x.csv")},
\code{saveRDS(m, "../out/model.rds")}). When the release is re-laid-out to
Psych-DS (data under \verb{data/}, per-study \verb{study-<group>/data/}), those paths no
lives, using the file→target plan \code{psychds_check} produced.
}
\details{
Matching is by \strong{basename} (a script's \code{../data/x.csv} and the repo's
\code{raw/x.csv} are the same file seen from different working directories, so the
prefix is ignored — the same choice \code{code_file_refs()} makes). When several
plan files share a basename (a \code{demographics.csv} in more than one study), the
ambiguity is resolved by \strong{study group}: the candidate whose target path is
in the same study as the script (derived from the paths via
\code{.data_group_from_path()}) is chosen. Only when that still leaves more than
one candidate is the reference left unrewritten and flagged.

A plan row for a converted tabular file (\code{psychds_check}'s \code{convert}/
\code{original_target} columns — a \code{.dta}/\code{.sav}/\code{.xlsx}/... source, whose
\code{target_path} is the re-exported \verb{_data.csv}, with the untouched original
kept alongside at \code{original_target}) is matched \strong{by the reference's own
extension}: a reference ending in the original's extension (\code{read_dta( "math.dta")}) is rewritten to \code{original_target}, since a format-specific
reader needs the real file, not the CSV re-export — using \code{target_path}
there would silently point the reader at bytes it cannot correctly parse.
A reference that already names the CSV form (\code{"math_data.csv"}, or a bare
\code{.csv} extension) still rewrites to \code{target_path} as before.

A path assembled at runtime with \code{sprintf()}/\code{paste()}/\code{paste0()}/
\code{file.path()} (see \code{\link[=.repro_format_call_refs]{.repro_format_call_refs()}}) is matched the same way,
using the format string's own literal basename (with any resolvable
placeholder substituted via \code{\link[=.repro_simple_string_vars]{.repro_simple_string_vars()}}) — but since the
true "reference" there is the WHOLE call, not the quoted substring inside
it, such rows carry \code{is_call = TRUE} and \code{ref} holds the full call text
(\code{sprintf("\%s/\%s/x.csv", wd, wd_data)}), so \code{\link[=repro_write_scripts]{repro_write_scripts()}} replaces
the entire expression rather than corrupting it with a substring swap that
would orphan the call's trailing arguments. A call whose placeholders could
not all be resolved is reported unmatched (\code{matched = FALSE}) rather than
guessed, and is separately flagged in the report as fragile authoring
practice regardless of whether it resolved.

A basename can also collide across plan rows for a reason that is NOT real
ambiguity at all: a paper linking several OSF components that mirror each
other (see \code{\link[=repro_materialize_layout]{repro_materialize_layout()}}'s own de-duplication for the
execution-time counterpart of this) commonly lists the identical physical
file more than once, and the study-grouping step can assign DIFFERENT
(and for all-but-one, WRONG) groups to different mirrors of the same file
— e.g. one mirror's folder structure carries a "Study 1" hint the others
lack, so only that one gets grouped correctly. When \code{structure_df} is
supplied, candidates are first collapsed by the CONTENT HASH of their
resolved source files: byte-identical candidates count as ONE, using
whichever one's own group agrees with the majority (ties broken by the
first occurrence) — confirmed against a real corpus paper where 4 mirrors
of the same \verb{Anonymized_2024-08-23.csv} were grouped ex1/ex1/ex1/ex3, which
made the existing "do all candidates already agree on one target"
shortcut fail (3 way agree, 1 disagrees) and fell through to \code{ambiguous = TRUE} even though there was only ever one real file. Without
\code{structure_df} (or when a candidate's source cannot be resolved/read),
this collapsing step is skipped and candidates are compared as before.
}
\examples{
plan <- data.frame(
  file_name = c("demographics.csv", "scores.csv"),
  target_path = c("study-ex1/data/study-demographics_data.csv",
                  "study-ex1/data/study-scores_data.csv"),
  current_path = c("ex1/demographics.csv", "ex1/scores.csv")
)
code_text <- c('d <- read.csv("data/demographics.csv")')
repro_rewrite_paths(code_text, "ex1/analysis.R", plan)
}
