% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_check_helpers.R
\name{data_check_spss_filter}
\alias{data_check_spss_filter}
\title{Flag an SPSS "Select Cases" filter variable}
\usage{
data_check_spss_filter(col, x)
}
\arguments{
\item{col}{the column name}

\item{x}{the column's values}
}
\value{
list(problem, message, values)
}
\description{
SPSS's Select Cases dialog creates a 0/1 variable named \verb{filter_$}
(mangled to \code{filter_.} or \code{filter_} by some importers). Its presence
matters to a re-user either way: if it is constant at 1 the file was
saved after deleting unselected cases, so the shared data are a
pre-filtered subset; if it varies, the reported analyses likely used only
the selected rows and the filter must be re-applied to reproduce them.
}
\keyword{internal}
