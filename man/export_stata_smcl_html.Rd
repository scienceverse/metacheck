% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stata.R
\name{export_stata_smcl_html}
\alias{export_stata_smcl_html}
\title{Export a Stata (.smcl) output log as standalone HTML}
\usage{
export_stata_smcl_html(path, out = NULL)
}
\arguments{
\item{path}{path to a \code{.smcl} file}

\item{out}{path to write the HTML file to; defaults to \code{path} with its
extension replaced by \code{.html}, written alongside the source file}
}
\value{
the path written to, invisibly
}
\description{
Builds an HTML page from what \code{\link[=import_stata_smcl]{import_stata_smcl()}} already decodes: one
heading per Stata command (its exact syntax, since a \code{.smcl} command echo
IS the syntax) and one \verb{<table>} per detected result. Unlike
\code{\link[=export_jasp_html]{export_jasp_html()}}/\code{\link[=export_omv_html]{export_omv_html()}} (which re-export an
already-rendered view) or \code{\link[=export_spv_html]{export_spv_html()}} (which renders decoded
charts as images), this has no figures to embed at all: a \code{.smcl} file
never contains chart image data (see the file header) -- a \verb{graph export} command is shown as recovered command text only.
}
