% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/archive-4tu.R
\name{researchdata4tu_file_download}
\alias{researchdata4tu_file_download}
\title{Download all files from a 4TU.ResearchData article}
\usage{
researchdata4tu_file_download(
  researchdata4tu_id,
  download_to = ".",
  max_file_size = 10,
  max_download_size = 100,
  unzip_types = NULL,
  pb = NULL
)
}
\arguments{
\item{researchdata4tu_id}{a 4TU.ResearchData article ID, uuid, URL, or DOI}

\item{download_to}{path to download to}

\item{max_file_size}{maximum file size to download (in MB) - set to NULL or
Inf for no restrictions}

\item{max_download_size}{maximum total size to download - set to NULL or
Inf for no restrictions}

\item{unzip_types}{file categories to extract from \code{.zip} files in the
article rather than downloading the zip whole, named as
\code{\link[=data_classify_files]{data_classify_files()}} names them: \code{"data"}, \code{"code"}, \code{"materials"},
\code{"documentation"}, \code{"output"}, \code{"unknown"}. More than one may be given.
\code{NULL} (the default) downloads zips whole and leaves them zipped, as
before. \code{max_file_size} applies to each extracted member rather than to
the archive.}

\item{pb}{a progress bar passed from another function}
}
\value{
data frame of file info. When members are extracted from a zip, its
row reports the zip with \code{extracted} giving the number of members written;
\code{size_on_disk} and \code{checksum_ok} are then \code{NA}, because what is on disk is
the members rather than the archive 4TU.ResearchData published a size and
MD5 for.
}
\description{
Thin wrapper around \code{\link[=figshare_file_download]{figshare_file_download()}} with \code{host = "data.4tu.nl"}
-- see the note at the top of this file. Creates a directory for the
article and downloads all of its files. Returns (invisibly) a data frame
with file info.
}
\details{
Uses \code{\link[=researchdata4tu_pat]{researchdata4tu_pat()}}, not \code{\link[=figshare_pat]{figshare_pat()}}, for authentication
(see that function's documentation for why the two are separate).
}
\examples{
\dontrun{
  researchdata4tu_file_download("16766929")

  # take only the data files out of any zip in the article
  researchdata4tu_file_download("16766929", unzip_types = "data")
}
}
