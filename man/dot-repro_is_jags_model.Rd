% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reproducibility_check.R
\name{.repro_is_jags_model}
\alias{.repro_is_jags_model}
\title{Recognise a JAGS/BUGS model-definition file}
\usage{
.repro_is_jags_model(code_text, file_name = "", other_code_text = NULL)
}
\arguments{
\item{code_text}{the file's raw text (character vector, one element per
line)}

\item{file_name}{the file's own name/path (checked for a \code{jags_script}/
\code{bugs_script}/\code{jags_model} path component)}

\item{other_code_text}{optional list of OTHER files' code text (e.g. the
paper's other R files) to scan for a \code{jags.model()}/\code{jags()}/\code{run.jags()}
call naming this file — \code{NULL} skips this (strongest) signal}
}
\value{
\code{TRUE} when any signal matches, else \code{FALSE}
}
\description{
A JAGS/BUGS model (\verb{model \{ ... \}}) is written in JAGS's own modelling
language, not R, and is meant to be passed as a text file to
\code{rjags::jags.model()}/\code{R2jags::jags()}/\code{runjags::run.jags()}, never parsed
as R directly — authors frequently save one with a \code{.R} extension, which
then fails \code{\link[=code_parse_r]{code_parse_r()}} and reads identically to a genuine broken R
script. Matched via three signals, any one of which is treated as
sufficient (the third — an actual calling reference from the paper's own
code — is the strongest, since it confirms the paper's own code treats
this file as a JAGS model rather than something to run directly):
}
\details{
\enumerate{
\item the file's first non-comment, non-blank content is the token \code{model}
immediately followed by \verb{\{};
\item its path contains a component named \code{jags_script}/\code{bugs_script}/
\code{jags_model} (case-insensitive);
\item it is referenced elsewhere in the SAME paper's own R code as the
\verb{file=}/first argument to \code{rjags::jags.model()}, \code{R2jags::jags()}, or
\code{runjags::run.jags()}.
}
}
\keyword{internal}
