% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scienceverse.R
\name{scienceverse_checks}
\alias{scienceverse_checks}
\title{Recover the checks result stored in a scienceverse archive}
\usage{
scienceverse_checks(con, doi)
}
\arguments{
\item{con}{a DBI connection to a scienceverse SQLite archive.}

\item{doi}{the paper identifier (the \code{doi} column / collection folder name).}
}
\value{
the checks result (a list of module outputs), or \code{NULL} if the
paper has no stored blob.
}
\description{
The \code{papers.checks_rds} column holds each paper's checks result as the exact
bytes of its \verb{logs/<doi>.rds} file (gzip-compressed, as \code{\link[=saveRDS]{saveRDS()}} writes).
This helper pulls that BLOB for one paper and reconstructs the original R
object, so callers do not have to know it is compressed.
}
\examples{
\dontrun{
con <- DBI::dbConnect(RSQLite::SQLite(), "scienceverse.sqlite")
res <- scienceverse_checks(con, "collabra.165")
res$modules$stat_check$table
}
}
