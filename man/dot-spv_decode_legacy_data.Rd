% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spv.R
\name{.spv_decode_legacy_data}
\alias{.spv_decode_legacy_data}
\title{Decode one legacy .spv raw case-data member}
\usage{
.spv_decode_legacy_data(raw)
}
\arguments{
\item{raw}{raw vector: the exact bytes of the \code{.bin} zip member.}
}
\value{
a list of sources, each \code{list(source_name, variables)} where
\code{variables} is a list of \code{list(var_name, values)}, \code{values} a list of
\code{list(d, s)} per case (numeric xor string; \code{NA} for the unused side).
\code{NULL} if the member cannot be decoded.
}
\description{
Reads the raw bytes of a legacy-format \code{dataPath} zip member: PSPP's
\code{spv_legacy_data_decode()}, ported. Returns the per-"source" (variable
group) case data needed to reconstruct a pivot table once cross-referenced
against the sibling \code{path} XML member's dimension/category structure — see
\code{\link[=.spv_decode_legacy_table]{.spv_decode_legacy_table()}}.
}
\keyword{internal}
