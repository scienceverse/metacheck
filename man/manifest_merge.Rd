% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_check_helpers.R
\name{manifest_merge}
\alias{manifest_merge}
\title{Merge fields into a metacheck manifest, preserving other sections}
\usage{
manifest_merge(path, patch)
}
\arguments{
\item{path}{the manifest file path (\verb{*.manifest.json})}

\item{patch}{a named list of top-level keys to set}
}
\value{
(invisibly) the manifest path
}
\description{
The per-paper \verb{*.manifest.json} is written by more than one module:
\code{data_check} records the files/provenance, and \code{code_check} records the
packages the code loads (a \code{code} section). Because each module rebuilds only
its own part of the document, a plain overwrite would let whichever module ran
last erase the other's section. This helper reads any existing manifest,
overlays \code{patch} at the top level (each key in \code{patch} replaces that key
wholesale; keys not in \code{patch} are kept untouched), and writes it back.
}
\details{
The rule that makes this safe against run order: each writer's \code{patch} must
contain only the keys it owns. \code{data_check} patches everything except \code{code};
\code{code_check} patches only \code{code}. Overlapping keys would still clobber.
}
