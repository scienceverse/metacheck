% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_check_helpers.R
\name{data_col_type}
\alias{data_col_type}
\title{Classify a single data column by rule}
\usage{
data_col_type(col_name, values)
}
\arguments{
\item{col_name}{the column's name (drives the ID-pattern rule)}

\item{values}{the column's values (a vector)}
}
\value{
a list with \code{col_type} (a value from \code{.data_check_col_types}, or
\code{NA} when only the LLM could decide), \code{ambiguous} (whether the LLM should
be consulted), \code{numeric_values} (numeric vector for stats, or \code{NULL}),
\code{n_coerced}, and \code{is_numeric}.
}
\description{
Rule order (ported from datacheck \code{classify_col_type_rules()}): all-NA →
empty; ID name pattern → id; 1 unique → constant; 2 unique → binary;
date-parseable → date; long strings → text; numeric → continuous (or
ambiguous integer, flagged for LLM); comma-decimal → continuous variants.
}
\examples{
data_col_type("age", c(23, 45, 31, 29))
data_col_type("subject_id", c("s01", "s02", "s03"))
}
\keyword{internal}
