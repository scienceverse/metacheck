% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/archive-osf.R
\name{osf_info}
\alias{osf_info}
\title{Retrieve info from the OSF by ID}
\usage{
osf_info(osf_url, id_col = 1, recursive = FALSE, pb = NULL, cache = FALSE)
}
\arguments{
\item{osf_url}{an OSF ID or URL, or a table containing them}

\item{id_col}{the index or name of the column that contains OSF IDs or URLs, if id is a table}

\item{recursive}{whether to retrieve all children}

\item{pb}{a progress bar passed from another function}

\item{cache}{if \code{TRUE}, also reuse a PERSISTENT, on-disk cached listing per
id (see \code{\link[=repo_info_cache]{repo_info_cache()}}) that survives a restart of the script --
separate from, and in addition to, this function's own in-memory,
session-only cache described above. Off by default.}
}
\value{
a data frame of information
}
\description{
Repository listings are cached for the session (keyed by URL and
\code{recursive}), so repeated calls do not re-query the OSF API; clear the cache
with \code{\link[=osf_cache_clear]{osf_cache_clear()}} or disable it with
\code{options(metacheck.osf.cache = FALSE)}.
}
\examples{
\dontrun{
# get info on one OSF node
osf_info("pngda")

# also get child nodes and files
osf_info("https://osf.io/6nt4v", recursive = TRUE)
}
}
